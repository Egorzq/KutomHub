local AutoExecuteURL = "" -- ВСТАВЬ СЮДА СВОЮ RAW-ССЫЛКУ С GITHUB ДЛЯ АВТОЗАПУСКА ПРИ ПЕРЕЗАХОДЕ

local unpack = unpack or table.unpack

local Players = game:GetService("Players")
local player = Players.LocalPlayer
local mouse = player:GetMouse()
local UIS = game:GetService("UserInputService")
local TS = game:GetService("TweenService")
local RS = game:GetService("RunService")
local Http = game:GetService("HttpService")
local TPS = game:GetService("TeleportService")
local Lighting = game:GetService("Lighting")
local AssetService = game:GetService("AssetService")
local AvatarEditorService = game:GetService("AvatarEditorService")
local VirtualUser = game:GetService("VirtualUser")

local currentAnimTrack = nil
local currentAnimId = nil
local isFastResetting = false
local fastResetCFrame = nil

local mm2EarlyRoles = {}
local isRoleEsp = false
local isGunEsp = false
local isAutoGrab = false

-- Переменные читов
local isAutoShoot = false
local isSilentAim = false
local isXRay = false
local isAutoEvade = false
local origXrayTrans = {}
local customFov = 70
local isFovEnabled = false

-- Функция определения ролей
local function getRoleInfo(p)
    if mm2EarlyRoles[p.Name] then
        local r = mm2EarlyRoles[p.Name]
        if r == "Murderer" then return "MURDERER", Color3.fromRGB(255, 50, 50) end
        if r == "Sheriff" or r == "Hero" then return "SHERIFF", Color3.fromRGB(50, 150, 255) end
        return "Innocent", Color3.fromRGB(50, 255, 50)
    end
    local bp = p:FindFirstChild("Backpack") 
    local char = p.Character 
    local mur, sher = false, false
    local function c(f)
        if not f then return end
        for _, v in ipairs(f:GetChildren()) do 
            if v:IsA("Tool") then 
                if v.Name == "Knife" then mur = true end 
                if v.Name == "Gun" or v.Name == "Revolver" then sher = true end 
            end 
        end
    end
    c(bp) c(char)
    if mur then return "MURDERER", Color3.fromRGB(255, 50, 50) end
    if sher then return "SHERIFF", Color3.fromRGB(50, 150, 255) end
    return "Innocent", Color3.fromRGB(50, 255, 50)
end

-- ================== СИСТЕМА SILENT AIM (ПЕРЕХВАТ) ==================
local function getTargetForSilentAim(isMurderer)
    local target = nil
    local dist = math.huge
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= player and p.Character and p.Character:FindFirstChild("HumanoidRootPart") and p.Character:FindFirstChild("Humanoid") and p.Character.Humanoid.Health > 0 then
            local r = getRoleInfo(p)
            if isMurderer then
                local d = (p.Character.HumanoidRootPart.Position - player.Character.HumanoidRootPart.Position).Magnitude
                if d < dist then dist = d; target = p.Character end
            else
                if r == "MURDERER" then return p.Character end
            end
        end
    end
    return target
end

pcall(function()
    local oldNamecall
    oldNamecall = hookmetamethod(game, "__namecall", function(self, ...)
        local method = getnamecallmethod()
        local args = {...}
        
        if isSilentAim and not checkcaller() then
            if tostring(self) == "ShootGun" and method == "InvokeServer" then
                local target = getTargetForSilentAim(false)
                if target and target:FindFirstChild("HumanoidRootPart") then
                    args[2] = target.HumanoidRootPart.Position
                    return oldNamecall(self, unpack(args))
                end
            elseif tostring(self) == "ThrowKnife" and method == "InvokeServer" then
                local target = getTargetForSilentAim(true)
                if target and target:FindFirstChild("HumanoidRootPart") then
                    args[1] = target.HumanoidRootPart.Position
                    return oldNamecall(self, unpack(args))
                end
            end
        end
        return oldNamecall(self, ...)
    end)
end)

-- ================== СОХРАНЕНИЕ ==================
local saveFileName = "MyCustomAnims.json"
local customAnims = {}
local customPacks = {}
local binds = {}
local allTeleports = {}
local mm2Tracks = {}

local defaultSetts = {
    rainbow = true, targetSpeed = 16, targetJump = 50, flySpeed = 50, goldPartDuration = 3,
    selectedPackData = nil, autoPlaySong = false, lastPlayedSongId = nil,
    bhopMaxSpeed = 100, toggles = {}
}
local mySetts = {}
for k, v in pairs(defaultSetts) do mySetts[k] = v end

pcall(function()
    if isfile and isfile(saveFileName) then
        local rawData = readfile(saveFileName)
        local data = Http:JSONDecode(rawData)
        if data then
            if #data > 0 and data[1] and data[1].id then customAnims = data else
                if data.customAnims then customAnims = data.customAnims end
                if data.customPacks then customPacks = data.customPacks end
                if data.binds then binds = data.binds end
                if data.mm2Tracks and #data.mm2Tracks > 0 then mm2Tracks = data.mm2Tracks end
                if data.allTeleports then allTeleports = data.allTeleports end
                if data.mySetts then 
                    for k, v in pairs(data.mySetts) do 
                        if k ~= "clicks" and k ~= "toggleKey" then mySetts[k] = v end
                    end 
                end
            end
        end
    end
end)

local placeKey = tostring(game.PlaceId)
local teleports = allTeleports[placeKey] or {}

local function saveData()
    allTeleports[placeKey] = teleports
    if writefile then 
        local dataToSave = {
            customAnims = customAnims, customPacks = customPacks, binds = binds, 
            allTeleports = allTeleports, mySetts = mySetts, mm2Tracks = mm2Tracks
        }
        pcall(function() writefile(saveFileName, Http:JSONEncode(dataToSave)) end) 
    end
end

-- ================== UI HELPER И ФУНКЦИИ ==================
local function make(cls, props, parent)
    local obj = Instance.new(cls)
    for k, v in pairs(props) do obj[k] = v end
    if parent then obj.Parent = parent end return obj
end

local function addCorner(p, r) return make("UICorner", {CornerRadius = UDim.new(0, r)}, p) end
local function addStroke(p, c, t) return make("UIStroke", {Color = c, Thickness = t, ApplyStrokeMode = Enum.ApplyStrokeMode.Border}, p) end
local function tween(obj, t, props) if obj and TS then TS:Create(obj, TweenInfo.new(t, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), props):Play() end end

local function tpToPos(cf)
    if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
        player.Character.HumanoidRootPart.CFrame = cf
    end
end

local isFly, fConn, bv, bg = false, nil, nil, nil
local function toggleFly()
    isFly = not isFly 
    local c = player.Character if not c then return end local hrp = c:FindFirstChild("HumanoidRootPart") if not hrp then return end
    if isFly then
        bv = make("BodyVelocity", {MaxForce = Vector3.new(9e9, 9e9, 9e9)}, hrp) bg = make("BodyGyro", {MaxTorque = Vector3.new(9e9, 9e9, 9e9), P = 10000}, hrp)
        local cam = workspace.CurrentCamera
        fConn = RS.RenderStepped:Connect(function()
            if not player.Character or not player.Character:FindFirstChild("HumanoidRootPart") then isFly = false fConn:Disconnect() return end
            bg.CFrame = cam.CFrame local mv = Vector3.new()
            if UIS:IsKeyDown(Enum.KeyCode.W) then mv = mv + cam.CFrame.LookVector end if UIS:IsKeyDown(Enum.KeyCode.S) then mv = mv - cam.CFrame.LookVector end
            if UIS:IsKeyDown(Enum.KeyCode.A) then mv = mv - cam.CFrame.RightVector end if UIS:IsKeyDown(Enum.KeyCode.D) then mv = mv + cam.CFrame.RightVector end
            bv.Velocity = mv.Magnitude > 0 and mv.Unit * mySetts.flySpeed or Vector3.zero
        end)
    else 
        if fConn then fConn:Disconnect() end if bv then bv:Destroy() end if bg then bg:Destroy() end 
    end
end

local playAnim, doFastReset

-- GUI Инстанс
local sgui = make("ScreenGui", {Name="SolaraKutomHub", ResetOnSpawn=false, IgnoreGuiInset=true})
pcall(function() sgui.Parent = game:GetService("CoreGui") end)
if not sgui.Parent then sgui.Parent = player:WaitForChild("PlayerGui") end

-- УВЕДОМЛЕНИЕ
local noticeFrame = make("Frame", {Size = UDim2.new(0, 340, 0, 45), AnchorPoint = Vector2.new(0.5, 0), Position = UDim2.new(0.5, 0, 1, 80), BackgroundColor3 = Color3.fromRGB(30, 180, 70), ZIndex = 9999}, sgui)
addCorner(noticeFrame, 6) addStroke(noticeFrame, Color3.fromRGB(255, 255, 255), 1)
local noticeText = make("TextLabel", {Size = UDim2.new(1, 0, 1, 0), BackgroundTransparency = 1, TextColor3 = Color3.fromRGB(255, 255, 255), Font = Enum.Font.GothamBold, TextSize = 14, TextXAlignment = Enum.TextXAlignment.Center, ZIndex = 10000}, noticeFrame)

local function showNotice(txt) 
    if not noticeText then return end
    noticeText.Text = txt 
    tween(noticeFrame, 0.4, {Position = UDim2.new(0.5, 0, 1, -60)}) 
    task.delay(2.5, function() tween(noticeFrame, 0.4, {Position = UDim2.new(0.5, 0, 1, 80)}) end) 
end

-- ================== ОСНОВНОЕ ОКНО ==================
local mainFrame = make("Frame", {Size = UDim2.new(0, 780, 0, 500), AnchorPoint = Vector2.new(0.5, 0.5), Position = UDim2.new(0.5, 0, 0.5, 0), BackgroundColor3 = Color3.fromRGB(15, 15, 18), Active = true, Draggable = true, ClipsDescendants = true, Visible = false}, sgui)
addStroke(mainFrame, Color3.fromRGB(45, 42, 55), 2) addCorner(mainFrame, 8) 

local topBar = make("Frame", {Size = UDim2.new(1, 0, 0, 32), BackgroundColor3 = Color3.fromRGB(18, 18, 22)}, mainFrame)
local logoLabel = make("TextLabel", {Size = UDim2.new(0, 200, 1, 0), Position = UDim2.new(0, 12, 0, 0), BackgroundTransparency = 1, TextColor3 = Color3.fromRGB(220, 100, 60), Text = "Universal KutomHub", Font = Enum.Font.GothamBold, TextSize = 14, TextXAlignment = Enum.TextXAlignment.Left}, topBar)

local closeBtn = make("TextButton", {Size = UDim2.new(0, 40, 1, 0), Position = UDim2.new(1, -40, 0, 0), BackgroundTransparency = 1, TextColor3 = Color3.fromRGB(200, 200, 200), Text = "X", Font = Enum.Font.GothamBold, TextSize = 14}, topBar)
local maxBtn = make("TextButton", {Size = UDim2.new(0, 40, 1, 0), Position = UDim2.new(1, -80, 0, 0), BackgroundTransparency = 1, TextColor3 = Color3.fromRGB(200, 200, 200), Text = "□", Font = Enum.Font.GothamBold, TextSize = 18}, topBar)
local minBtn = make("TextButton", {Size = UDim2.new(0, 40, 1, 0), Position = UDim2.new(1, -120, 0, 0), BackgroundTransparency = 1, TextColor3 = Color3.fromRGB(200, 200, 200), Text = "-", Font = Enum.Font.GothamBold, TextSize = 20}, topBar)

local isHidden = true
local isFullscreen = false

local function toggleMenu()
    isHidden = not isHidden
    if isHidden then tween(mainFrame, 0.4, {Position = UDim2.new(0.5, 0, 1.5, 0)}) task.delay(0.4, function() if isHidden then mainFrame.Visible = false end end)
    else mainFrame.Visible = true tween(mainFrame, 0.4, {Position = UDim2.new(0.5, 0, 0.5, 0)}) end
end

minBtn.MouseButton1Click:Connect(toggleMenu)
maxBtn.MouseButton1Click:Connect(function()
    isFullscreen = not isFullscreen
    if isFullscreen then maxBtn.Text = "❐" mainFrame.Draggable = false tween(mainFrame, 0.3, {Size = UDim2.new(0.85, 0, 0.85, 0)})
    else maxBtn.Text = "□" mainFrame.Draggable = true tween(mainFrame, 0.3, {Size = UDim2.new(0, 780, 0, 500)}) end
end)
closeBtn.MouseButton1Click:Connect(function() if currentAnimTrack then currentAnimTrack:Stop() end tween(mainFrame, 0.3, {Position = UDim2.new(0.5, 0, 1.5, 0)}) task.delay(0.3, function() sgui:Destroy() end) end)

-- Левая панель иконок
local sideBar = make("Frame", {Size = UDim2.new(0, 40, 1, -32), Position = UDim2.new(0, 0, 0, 32), BackgroundColor3 = Color3.fromRGB(12, 12, 15)}, mainFrame)
make("UIListLayout", {SortOrder = Enum.SortOrder.LayoutOrder, Padding = UDim.new(0, 5)}, sideBar)
local contentContainer = make("Frame", {Size = UDim2.new(1, -40, 1, -32), Position = UDim2.new(0, 40, 0, 32), BackgroundColor3 = Color3.fromRGB(18, 18, 22)}, mainFrame)

local views = {}
local sidebarBtns = {}

local function createView(name)
    local f = make("ScrollingFrame", {Size = UDim2.new(1, -10, 1, -10), Position = UDim2.new(0, 10, 0, 5), BackgroundTransparency = 1, ScrollBarThickness = 4, ScrollBarImageColor3 = Color3.fromRGB(50, 50, 50), AutomaticCanvasSize = Enum.AutomaticSize.Y, CanvasSize = UDim2.new(0, 0, 0, 0), Visible = false}, contentContainer)
    make("UIListLayout", {SortOrder = Enum.SortOrder.LayoutOrder, Padding = UDim.new(0, 8)}, f)
    views[name] = f return f
end

local vEmotes = make("Frame", {Size = UDim2.new(1, -10, 1, -10), Position = UDim2.new(0, 10, 0, 5), BackgroundTransparency = 1, Visible = false}, contentContainer)
views["Emotes"] = vEmotes

local vMove = make("Frame", {Size = UDim2.new(1, -10, 1, -10), Position = UDim2.new(0, 10, 0, 5), BackgroundTransparency = 1, Visible = false}, contentContainer)
views["Movements"] = vMove

local vMm2 = createView("MM2")
local vVip = createView("Vip")
local vTools = createView("Tools")
local vOther = createView("Other")
local vSetts = createView("Settings")

local tabNamesRus = {Emotes = "Эмоции", Movements = "Движения", MM2 = "MM2 Читы", Vip = "VIP-Меню", Tools = "Инструменты", Other = "Прочее", Settings = "Настройки"}

local function switchTab(name)
    for k, v in pairs(views) do 
        if k == name then v.Visible = true v.Position = UDim2.new(0, 15, 0, 5) tween(v, 0.3, {Position = UDim2.new(0, 5, 0, 5)}) else v.Visible = false end
    end
    for k, b in pairs(sidebarBtns) do tween(b, 0.2, {TextColor3 = (k == name) and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(150, 150, 165)}) end
end

local function makeSidebarIcon(icon, tabName)
    local b = make("TextButton", {Size = UDim2.new(0, 40, 0, 40), BackgroundTransparency = 1, TextColor3 = Color3.fromRGB(150, 150, 165), Text = icon, Font = Enum.Font.GothamBold, TextSize = 16}, sideBar)
    sidebarBtns[tabName] = b
    local tooltip = make("TextLabel", {Size = UDim2.new(0, 110, 0, 24), Position = UDim2.new(1, 5, 0.5, 0), AnchorPoint = Vector2.new(0, 0.5), BackgroundColor3 = Color3.fromRGB(25, 25, 30), TextColor3 = Color3.fromRGB(255,255,255), Text = tabNamesRus[tabName] or tabName, Font = Enum.Font.GothamBold, TextSize = 11, Visible = false, ZIndex = 1000, BackgroundTransparency = 1, TextTransparency = 1}, b) addCorner(tooltip, 4) addStroke(tooltip, Color3.fromRGB(60,60,70), 1)
    local isHovered = false
    b.MouseEnter:Connect(function() isHovered = true tooltip.Visible = true tween(tooltip, 0.15, {BackgroundTransparency = 0, TextTransparency = 0}) end)
    b.MouseLeave:Connect(function() isHovered = false tween(tooltip, 0.15, {BackgroundTransparency = 1, TextTransparency = 1}) task.delay(0.15, function() if not isHovered then tooltip.Visible = false end end) end)
    b.MouseButton1Click:Connect(function() tween(b, 0.1, {TextSize = 12}) task.delay(0.1, function() tween(b, 0.1, {TextSize = 16}) end) switchTab(tabName) end)
    return b
end

makeSidebarIcon("🎵", "Emotes") makeSidebarIcon("🚶", "Movements") makeSidebarIcon("🔪", "MM2") makeSidebarIcon("⭐", "Vip") makeSidebarIcon("🛠️", "Tools") makeSidebarIcon("📦", "Other") makeSidebarIcon("⚙️", "Settings")

-- КОМПОНЕНТЫ ИНТЕРФЕЙСА ДЛЯ ВКЛАДОК
local function CreateLabel(page, text, layoutOrder) 
    return make("TextLabel", {Size = UDim2.new(1, -10, 0, 25), BackgroundTransparency = 1, Text = text, TextColor3 = Color3.fromRGB(180, 180, 190), Font = Enum.Font.GothamBold, TextSize = 13, TextXAlignment = Enum.TextXAlignment.Left, LayoutOrder = layoutOrder or 0}, page) 
end
local function CreateButton(page, text, callback, color, layoutOrder) 
    local Btn = make("TextButton", {Size = UDim2.new(1, -10, 0, 34), BackgroundColor3 = color or Color3.fromRGB(25, 25, 32), Text = text, TextColor3 = Color3.fromRGB(220, 220, 220), Font = Enum.Font.GothamBold, TextSize = 12, LayoutOrder = layoutOrder or 0}, page)
    addCorner(Btn, 6) Btn.MouseButton1Click:Connect(callback) return Btn
end
local function CreateTextBox(page, placeholder, text, layoutOrder)
    local Box = make("TextBox", {Size = UDim2.new(1, -10, 0, 34), BackgroundColor3 = Color3.fromRGB(20, 20, 26), PlaceholderText = placeholder, Text = text or "", TextColor3 = Color3.fromRGB(255, 255, 255), Font = Enum.Font.Gotham, TextSize = 12, LayoutOrder = layoutOrder or 0}, page)
    addCorner(Box, 6) return Box
end

-- ================== ЕДИНАЯ СИСТЕМА БИНДОВ И АВТО-СОХРАНЕНИЯ ==================
local isBindingLoc = false
local bindIdLoc = nil
local bindBtnLoc = nil

local function setupBind(btn, id)
    local k = binds[id] 
    if btn then 
        btn.Text = k and ("["..k.."]") or "None" 
        btn.TextColor3 = k and Color3.fromRGB(150, 255, 150) or Color3.fromRGB(200, 200, 200) 
    end
    if btn then 
        btn.MouseButton1Click:Connect(function() 
            if isBindingLoc then return end 
            isBindingLoc = true bindIdLoc = id bindBtnLoc = btn btn.Text = "..." tween(btn, 0.2, {TextColor3 = Color3.fromRGB(255, 200, 50)})
        end) 
    end
end

UIS.InputBegan:Connect(function(inp, gp)
    if isBindingLoc and inp.UserInputType == Enum.UserInputType.Keyboard then
        local k = inp.KeyCode.Name 
        if k == "Escape" or k == "Backspace" then binds[bindIdLoc] = nil else binds[bindIdLoc] = k end
        
        if bindBtnLoc then
            bindBtnLoc.Text = binds[bindIdLoc] and ("["..binds[bindIdLoc].."]") or "None"
            bindBtnLoc.TextColor3 = binds[bindIdLoc] and Color3.fromRGB(150, 255, 150) or Color3.fromRGB(200, 200, 200)
        end
        isBindingLoc = false saveData() return
    end

    if gp then return end
    
    if inp.UserInputType == Enum.UserInputType.Keyboard then
        local k = inp.KeyCode.Name
        if k == "Insert" then toggleMenu() return end
        
        for id, bk in pairs(binds) do
            if bk == k then
                if id == "VIP_Fly" then 
                    toggleFly() 
                elseif id == "Other_FastReset" then 
                    if doFastReset then doFastReset() end
                elseif string.sub(id, 1, 3) == "TP_" then 
                    local tName = string.sub(id, 4) 
                    if teleports[tName] then tpToPos(CFrame.new(unpack(teleports[tName].pos))) end 
                else 
                    if playAnim then playAnim(id) end
                end 
                break
            end
        end
    end
end)

local function CreateToggleWithBind(page, text, bindId, initialState, callback, layoutOrder)
    if not mySetts.toggles then mySetts.toggles = {} end
    
    local enabled = mySetts.toggles[text]
    if enabled == nil then
        enabled = initialState or false
        mySetts.toggles[text] = enabled
    end

    local Row = make("Frame", {Size = UDim2.new(1, -10, 0, 34), BackgroundTransparency = 1, LayoutOrder = layoutOrder or 0}, page)
    local ToggleBtn = make("TextButton", {Size = UDim2.new(1, bindId and -50 or 0, 1, 0), BackgroundColor3 = enabled and Color3.fromRGB(40, 160, 60) or Color3.fromRGB(25, 25, 32), Text = text .. (enabled and ": ВКЛ" or ": ВЫКЛ"), TextColor3 = Color3.fromRGB(220, 220, 220), Font = Enum.Font.GothamBold, TextSize = 12}, Row)
    addCorner(ToggleBtn, 6)
    
    local BindBtn = nil
    if bindId then
        BindBtn = make("TextButton", {Size = UDim2.new(0, 45, 1, 0), Position = UDim2.new(1, -45, 0, 0), BackgroundColor3 = Color3.fromRGB(35, 35, 45), Text = "None", TextColor3 = Color3.fromRGB(200, 200, 200), Font = Enum.Font.GothamBold, TextSize = 11}, Row)
        addCorner(BindBtn, 6)
        local k = binds[bindId]
        if k then BindBtn.Text = "["..k.."]" BindBtn.TextColor3 = Color3.fromRGB(150, 255, 150) end
    end

    local isLocalBinding = false

    local function SetState(state) 
        enabled = state 
        mySetts.toggles[text] = enabled
        saveData()
        
        ToggleBtn.Text = text .. (enabled and ": ВКЛ" or ": ВЫКЛ")
        tween(ToggleBtn, 0.2, {BackgroundColor3 = enabled and Color3.fromRGB(40, 160, 60) or Color3.fromRGB(25, 25, 32)})
        callback(enabled) 
    end
    
    ToggleBtn.MouseButton1Click:Connect(function() SetState(not enabled) end)
    
    if BindBtn then
        BindBtn.MouseButton1Click:Connect(function() 
            isLocalBinding = true 
            BindBtn.Text = "..." 
            tween(BindBtn, 0.2, {TextColor3 = Color3.fromRGB(255, 200, 50)})
        end)
        UIS.InputBegan:Connect(function(input, gpe)
            if isLocalBinding and input.UserInputType == Enum.UserInputType.Keyboard then
                local keyName = input.KeyCode.Name
                if keyName == "Escape" or keyName == "Backspace" then 
                    binds[bindId] = nil 
                    BindBtn.Text = "None"
                    BindBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
                else 
                    binds[bindId] = keyName 
                    BindBtn.Text = "["..keyName.."]"
                    BindBtn.TextColor3 = Color3.fromRGB(150, 255, 150)
                end
                isLocalBinding = false
                saveData()
            elseif not gpe and not isLocalBinding and binds[bindId] and input.KeyCode.Name == binds[bindId] then
                SetState(not enabled)
            end
        end)
    end
    
    task.spawn(function() callback(enabled) end)
    
    return SetState
end

local function CreateActionRow(page, inputHolder, btn1Txt, btn1Cb, btn2Txt, btn2Cb)
    local Row = make("Frame", {Size = UDim2.new(1, -10, 0, 35), BackgroundTransparency = 1}, page)
    local Inp = make("TextBox", {Size = UDim2.new(0.4, -5, 1, 0), BackgroundColor3 = Color3.fromRGB(25, 25, 32), PlaceholderText = inputHolder, PlaceholderColor3 = Color3.fromRGB(200, 200, 200), Text = "", TextColor3 = Color3.fromRGB(255, 255, 255), Font = Enum.Font.Gotham, TextSize = 12}, Row) addCorner(Inp, 6)
    local Btn1 = make("TextButton", {Size = UDim2.new(0.4, -5, 1, 0), Position = UDim2.new(0.4, 5, 0, 0), BackgroundColor3 = Color3.fromRGB(40, 120, 200), Text = btn1Txt, TextColor3 = Color3.fromRGB(255, 255, 255), Font = Enum.Font.GothamBold, TextSize = 12}, Row) addCorner(Btn1, 6)
    Btn1.MouseButton1Click:Connect(function() btn1Cb(Inp.Text) end)

    if btn2Txt then
        local Btn2 = make("TextButton", {Size = UDim2.new(0.2, -5, 1, 0), Position = UDim2.new(0.8, 5, 0, 0), BackgroundColor3 = Color3.fromRGB(180, 50, 50), Text = btn2Txt, TextColor3 = Color3.fromRGB(255, 255, 255), Font = Enum.Font.GothamBold, TextSize = 12}, Row) addCorner(Btn2, 6)
        Btn2.MouseButton1Click:Connect(function() btn2Cb(Inp.Text) end)
    else
        Inp.Size = UDim2.new(0.6, -5, 1, 0) Btn1.Size = UDim2.new(0.4, 0, 1, 0) Btn1.Position = UDim2.new(0.6, 5, 0, 0)
    end
end

-- ================== 1. ЭМОЦИИ ==================
do
    local EmotesTop = make("Frame", {Size = UDim2.new(1, -10, 0, 30), BackgroundTransparency = 1}, vEmotes)
    local btnTabSearch = make("TextButton", {Size = UDim2.new(0.5, -5, 1, 0), BackgroundColor3 = Color3.fromRGB(40, 120, 200), Text = "🔍 Поиск", TextColor3 = Color3.fromRGB(255,255,255), Font = Enum.Font.GothamBold, TextSize = 12}, EmotesTop) addCorner(btnTabSearch, 6)
    local btnTabSaved = make("TextButton", {Size = UDim2.new(0.5, -5, 1, 0), Position = UDim2.new(0.5, 5, 0, 0), BackgroundColor3 = Color3.fromRGB(25, 25, 32), Text = "⭐ Сохраненные", TextColor3 = Color3.fromRGB(200,200,200), Font = Enum.Font.GothamBold, TextSize = 12}, EmotesTop) addCorner(btnTabSaved, 6)

    local vEmotesSearch = make("ScrollingFrame", {Size = UDim2.new(1, 0, 1, -35), Position = UDim2.new(0, 0, 0, 35), BackgroundTransparency = 1, ScrollBarThickness = 4, AutomaticCanvasSize = Enum.AutomaticSize.Y, CanvasSize = UDim2.new(0,0,0,0)}, vEmotes)
    local vEmotesSaved = make("ScrollingFrame", {Size = UDim2.new(1, 0, 1, -35), Position = UDim2.new(0, 0, 0, 35), BackgroundTransparency = 1, ScrollBarThickness = 4, AutomaticCanvasSize = Enum.AutomaticSize.Y, CanvasSize = UDim2.new(0,0,0,0), Visible = false}, vEmotes)

    btnTabSearch.MouseButton1Click:Connect(function()
        vEmotesSearch.Visible = true vEmotesSaved.Visible = false
        tween(btnTabSearch, 0.2, {BackgroundColor3 = Color3.fromRGB(40, 120, 200), TextColor3 = Color3.fromRGB(255,255,255)})
        tween(btnTabSaved, 0.2, {BackgroundColor3 = Color3.fromRGB(25, 25, 32), TextColor3 = Color3.fromRGB(200,200,200)})
    end)
    btnTabSaved.MouseButton1Click:Connect(function()
        vEmotesSaved.Visible = true vEmotesSearch.Visible = false
        tween(btnTabSaved, 0.2, {BackgroundColor3 = Color3.fromRGB(40, 120, 200), TextColor3 = Color3.fromRGB(255,255,255)})
        tween(btnTabSearch, 0.2, {BackgroundColor3 = Color3.fromRGB(25, 25, 32), TextColor3 = Color3.fromRGB(200,200,200)})
    end)

    local isLoopAnim = true
    playAnim = function(id)
        local char = player.Character if not char then return end 
        local hum = char:FindFirstChildOfClass("Humanoid") if not hum then return end
        local anim = hum:FindFirstChildOfClass("Animator") or Instance.new("Animator", hum)
        
        for _, track in ipairs(anim:GetPlayingAnimationTracks()) do track:Stop() end
        if currentAnimId == id then currentAnimId = nil return end
        
        local animIdToPlay = string.match(id, "^rbxassetid://") and id or "rbxassetid://"..id
        pcall(function()
            local rawId = string.match(id, "%d+")
            local objects = game:GetObjects("rbxassetid://" .. rawId)
            if objects and objects[1] then
                local animObj = objects[1]:IsA("Animation") and objects[1] or objects[1]:FindFirstChildWhichIsA("Animation", true)
                if animObj then animIdToPlay = animObj.AnimationId end
            end
        end)
        
        local a = Instance.new("Animation") 
        a.AnimationId = animIdToPlay
        currentAnimTrack = anim:LoadAnimation(a) 
        currentAnimTrack.Priority = Enum.AnimationPriority.Action 
        currentAnimTrack.Looped = isLoopAnim 
        currentAnimTrack:Play() 
        currentAnimId = id
    end

    make("UIListLayout", {SortOrder = Enum.SortOrder.LayoutOrder, Padding = UDim.new(0, 8)}, vEmotesSearch)
    local searchRow = make("Frame", {Size = UDim2.new(1, -10, 0, 35), BackgroundTransparency = 1, LayoutOrder = 1}, vEmotesSearch)
    local searchInp = make("TextBox", {Size = UDim2.new(0.65, -5, 1, 0), BackgroundColor3 = Color3.fromRGB(20, 20, 26), PlaceholderText = "Англ. название (напр: Dance)...", Text = "", TextColor3 = Color3.fromRGB(255, 255, 255), Font = Enum.Font.Gotham, TextSize = 12}, searchRow) addCorner(searchInp, 6)
    local searchBtn = make("TextButton", {Size = UDim2.new(0.35, 0, 1, 0), Position = UDim2.new(0.65, 5, 0, 0), BackgroundColor3 = Color3.fromRGB(150, 50, 200), Text = "Искать", TextColor3 = Color3.fromRGB(255, 255, 255), Font = Enum.Font.GothamBold, TextSize = 12}, searchRow) addCorner(searchBtn, 6)
    
    local searchResultsContainer = make("Frame", {Size = UDim2.new(1, -10, 0, 0), AutomaticSize = Enum.AutomaticSize.Y, BackgroundTransparency = 1, LayoutOrder = 2}, vEmotesSearch)
    make("UIListLayout", {SortOrder = Enum.SortOrder.LayoutOrder, Padding = UDim.new(0, 5)}, searchResultsContainer)
    
    local function renderEmotesUI() end

    searchBtn.MouseButton1Click:Connect(function()
        for _, child in ipairs(searchResultsContainer:GetChildren()) do if child:IsA("Frame") then child:Destroy() end end
        local q = searchInp.Text
        if q == "" then return end
        showNotice("Ищем в каталоге...")
        
        task.spawn(function()
            local success, result = pcall(function()
                local searchParams = CatalogSearchParams.new()
                searchParams.SearchKeyword = q
                searchParams.AssetTypes = {Enum.AvatarAssetType.EmoteAnimation}
                searchParams.SortType = Enum.CatalogSortType.Relevance
                return AvatarEditorService:SearchCatalog(searchParams)
            end)
            
            if success and result then
                local items = result:GetCurrentPage()
                if #items > 0 then
                    showNotice("Найдено: " .. #items)
                    for _, item in ipairs(items) do
                        local assetId = item.Id
                        local name = item.Name
                        
                        local Row = make("Frame", {Size = UDim2.new(1, 0, 0, 44), BackgroundColor3 = Color3.fromRGB(25, 25, 32)}, searchResultsContainer) addCorner(Row, 6)
                        local Icon = make("ImageLabel", {Size = UDim2.new(0, 36, 0, 36), Position = UDim2.new(0, 4, 0.5, -18), BackgroundColor3 = Color3.fromRGB(40,40,45), BackgroundTransparency = 0, Image = "rbxthumb://type=Asset&id=" .. tostring(assetId) .. "&w=150&h=150"}, Row) addCorner(Icon, 6)
                        local nameLbl = make("TextLabel", {Size = UDim2.new(0.5, -45, 1, 0), Position = UDim2.new(0, 45, 0, 0), BackgroundTransparency=1, TextColor3=Color3.fromRGB(255,255,255), Text=name, Font=Enum.Font.Gotham, TextSize=11, TextXAlignment=Enum.TextXAlignment.Left, TextTruncate=Enum.TextTruncate.AtEnd}, Row)
                        
                        local playCatBtn = make("TextButton", {Size=UDim2.new(0.2, 0, 0, 28), Position=UDim2.new(0.55, 0, 0.5, -14), BackgroundColor3=Color3.fromRGB(40,120,200), Text="▶ Играть", TextColor3=Color3.fromRGB(255,255,255), Font=Enum.Font.GothamBold, TextSize=10}, Row) addCorner(playCatBtn, 4)
                        local saveCatBtn = make("TextButton", {Size=UDim2.new(0.2, 0, 0, 28), Position=UDim2.new(0.78, 0, 0.5, -14), BackgroundColor3=Color3.fromRGB(40,160,60), Text="➕ Сохр.", TextColor3=Color3.fromRGB(255,255,255), Font=Enum.Font.GothamBold, TextSize=10}, Row) addCorner(saveCatBtn, 4)
                        
                        playCatBtn.MouseButton1Click:Connect(function() playAnim(tostring(assetId)) end)
                        saveCatBtn.MouseButton1Click:Connect(function() 
                            local idStr = tostring(assetId)
                            local exists = false
                            for _, v in ipairs(customAnims) do if v.id == idStr then exists = true break end end
                            if not exists then 
                                table.insert(customAnims, {name = name, id = idStr}) 
                                saveData() renderEmotesUI() showNotice("Сохранено!") 
                            else showNotice("Уже в списке!") end
                        end)
                    end
                else showNotice("Ничего не найдено!") end
            else showNotice("Сбой поиска в каталоге!") end
        end)
    end)

    make("UIListLayout", {SortOrder = Enum.SortOrder.LayoutOrder, Padding = UDim.new(0, 8)}, vEmotesSaved)
    CreateToggleWithBind(vEmotesSaved, "Зациклить эмоции", "Emotes_Loop", true, function(state)
        isLoopAnim = state
        if currentAnimTrack then currentAnimTrack.Looped = isLoopAnim end
    end, 1)

    CreateButton(vEmotesSaved, "⏹ Остановить анимацию", function()
        if currentAnimTrack then currentAnimTrack:Stop() currentAnimTrack = nil end
        currentAnimId = nil
        local char = player.Character 
        if char then
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum then
                local animator = hum:FindFirstChildOfClass("Animator")
                if animator then for _, track in ipairs(animator:GetPlayingAnimationTracks()) do if track.Priority == Enum.AnimationPriority.Action then track:Stop() end end end
            end
        end
    end, Color3.fromRGB(180, 50, 50), 2)

    local emotesContainer = make("Frame", {Size = UDim2.new(1,-10,0,0), AutomaticSize = Enum.AutomaticSize.Y, BackgroundTransparency = 1, LayoutOrder = 3}, vEmotesSaved)
    make("UIListLayout", {SortOrder = Enum.SortOrder.LayoutOrder, Padding = UDim.new(0, 5)}, emotesContainer)

    local function createEmoteRow(name, id)
        local Row = make("Frame", {Size = UDim2.new(1, 0, 0, 35), BackgroundTransparency = 1}, emotesContainer)
        local PlayBtn = make("TextButton", {Size = UDim2.new(1, -85, 1, 0), BackgroundColor3 = Color3.fromRGB(40, 40, 48), Text = "⭐ " .. name, TextColor3 = Color3.fromRGB(220, 220, 220), Font = Enum.Font.GothamSemibold, TextSize = 13}, Row) addCorner(PlayBtn, 6)
        local BindBtn = make("TextButton", {Size = UDim2.new(0, 35, 1, 0), Position = UDim2.new(1, -80, 0, 0), BackgroundColor3 = Color3.fromRGB(60, 60, 70), Font = Enum.Font.GothamBold, TextSize = 11}, Row) addCorner(BindBtn, 6)

        setupBind(BindBtn, id)
        PlayBtn.MouseButton1Click:Connect(function() playAnim(id) end)

        local DelBtn = make("TextButton", {Size = UDim2.new(0, 35, 1, 0), Position = UDim2.new(1, -40, 0, 0), BackgroundColor3 = Color3.fromRGB(180, 50, 50), Text = "🗑️", TextColor3 = Color3.fromRGB(255, 255, 255), Font = Enum.Font.GothamBold, TextSize = 12}, Row) addCorner(DelBtn, 6)
        DelBtn.MouseButton1Click:Connect(function() 
            for i, v in ipairs(customAnims) do if v.id == id then table.remove(customAnims, i) break end end 
            binds[id] = nil saveData() Row:Destroy()
        end)
    end

    renderEmotesUI = function()
        for _, child in ipairs(emotesContainer:GetChildren()) do if child:IsA("Frame") then child:Destroy() end end
        for _, v in ipairs(customAnims) do createEmoteRow(v.name, v.id) end
    end
    renderEmotesUI()

    CreateLabel(vEmotesSaved, "Добавить свою эмоцию вручную", 4)
    local emoNameInp = CreateTextBox(vEmotesSaved, "Название...", "", 5)
    local emoIdInp = CreateTextBox(vEmotesSaved, "ID Анимации...", "", 6)

    CreateButton(vEmotesSaved, "➕ Добавить / Переименовать", function()
        local n, i = emoNameInp.Text, emoIdInp.Text
        if n ~= "" and i ~= "" then
            local exists = false
            for _, v in ipairs(customAnims) do if v.id == i then v.name = n exists = true break end end
            if not exists then table.insert(customAnims, {name = n, id = i}) end
            saveData() renderEmotesUI() emoNameInp.Text = "" emoIdInp.Text = "" showNotice(exists and "Эмоция переименована!" or "Эмоция добавлена!")
        end
    end, Color3.fromRGB(40, 120, 200), 7)
end

-- ================== 2. ДВИЖЕНИЯ ==================
do
    local MoveTop = make("Frame", {Size = UDim2.new(1, -10, 0, 30), BackgroundTransparency = 1}, vMove)
    local btnMoveSearch = make("TextButton", {Size = UDim2.new(0.5, -5, 1, 0), BackgroundColor3 = Color3.fromRGB(40, 120, 200), Text = "🔍 Поиск Паков", TextColor3 = Color3.fromRGB(255,255,255), Font = Enum.Font.GothamBold, TextSize = 12}, MoveTop) addCorner(btnMoveSearch, 6)
    local btnMoveSaved = make("TextButton", {Size = UDim2.new(0.5, -5, 1, 0), Position = UDim2.new(0.5, 5, 0, 0), BackgroundColor3 = Color3.fromRGB(25, 25, 32), Text = "⭐ Сохраненные", TextColor3 = Color3.fromRGB(200,200,200), Font = Enum.Font.GothamBold, TextSize = 12}, MoveTop) addCorner(btnMoveSaved, 6)

    local vMoveSearch = make("ScrollingFrame", {Size = UDim2.new(1, 0, 1, -35), Position = UDim2.new(0, 0, 0, 35), BackgroundTransparency = 1, ScrollBarThickness = 4, AutomaticCanvasSize = Enum.AutomaticSize.Y, CanvasSize = UDim2.new(0,0,0,0)}, vMove)
    local vMoveSaved = make("ScrollingFrame", {Size = UDim2.new(1, 0, 1, -35), Position = UDim2.new(0, 0, 0, 35), BackgroundTransparency = 1, ScrollBarThickness = 4, AutomaticCanvasSize = Enum.AutomaticSize.Y, CanvasSize = UDim2.new(0,0,0,0), Visible = false}, vMove)

    btnMoveSearch.MouseButton1Click:Connect(function()
        vMoveSearch.Visible = true vMoveSaved.Visible = false
        tween(btnMoveSearch, 0.2, {BackgroundColor3 = Color3.fromRGB(40, 120, 200), TextColor3 = Color3.fromRGB(255,255,255)})
        tween(btnMoveSaved, 0.2, {BackgroundColor3 = Color3.fromRGB(25, 25, 32), TextColor3 = Color3.fromRGB(200,200,200)})
    end)
    btnMoveSaved.MouseButton1Click:Connect(function()
        vMoveSaved.Visible = true vMoveSearch.Visible = false
        tween(btnMoveSaved, 0.2, {BackgroundColor3 = Color3.fromRGB(40, 120, 200), TextColor3 = Color3.fromRGB(255,255,255)})
        tween(btnMoveSearch, 0.2, {BackgroundColor3 = Color3.fromRGB(25, 25, 32), TextColor3 = Color3.fromRGB(200,200,200)})
    end)

    local function setMove(st, an, id) 
        if not id or id == "" then return end 
        local c = player.Character 
        if c and c:FindFirstChild("Animate") then 
            local o = c.Animate:FindFirstChild(st) 
            if o and o:FindFirstChild(an) then 
                local animIdToPlay = string.match(id, "^rbxassetid://") and id or "rbxassetid://"..id
                pcall(function()
                    local rawId = string.match(id, "%d+")
                    local objects = game:GetObjects("rbxassetid://" .. rawId)
                    if objects and objects[1] then
                        local animObj = objects[1]:IsA("Animation") and objects[1] or objects[1]:FindFirstChildWhichIsA("Animation", true)
                        if animObj then animIdToPlay = animObj.AnimationId end
                    end
                end)
                o[an].AnimationId = animIdToPlay 
            end 
        end 
    end

    local function applyMove(i, w, r, j, f) 
        setMove("idle", "Animation1", i) 
        setMove("idle", "Animation2", i) 
        setMove("walk", "WalkAnim", w) 
        setMove("run", "RunAnim", r) 
        setMove("jump", "JumpAnim", j) 
        setMove("fall", "FallAnim", f) 
    end

    local function applyBundle(bundleId, bundleName)
        showNotice("Распаковка пака " .. bundleName .. "...")
        task.spawn(function()
            local s2, details = pcall(function() return AssetService:GetBundleDetailsAsync(bundleId) end)
            if s2 and details and details.Items then
                local idleId, walkId, runId, jumpId, fallId
                for _, subItem in ipairs(details.Items) do
                    local n = subItem.Name:lower()
                    local idStr = tostring(subItem.Id)
                    if n:find("idle") then idleId = idStr
                    elseif n:find("walk") then walkId = idStr
                    elseif n:find("run") then runId = idStr
                    elseif n:find("jump") then jumpId = idStr
                    elseif n:find("fall") or n:find("pose") then fallId = idStr end
                end
                idleId = idleId or details.Items[1] and tostring(details.Items[1].Id)
                walkId = walkId or idleId runId = runId or walkId jumpId = jumpId or idleId fallId = fallId or jumpId
                mySetts.selectedPackData = {idleId, walkId, runId, jumpId, fallId}
                saveData() applyMove(idleId, walkId, runId, jumpId, fallId)
                showNotice("Пак " .. bundleName .. " успешно применен!")
            else showNotice("⚠️ Не удалось распаковать пак!") end
        end)
    end

    make("UIListLayout", {SortOrder = Enum.SortOrder.LayoutOrder, Padding = UDim.new(0, 8)}, vMoveSearch)
    local bundleSearchRow = make("Frame", {Size = UDim2.new(1, -10, 0, 35), BackgroundTransparency = 1, LayoutOrder = 1}, vMoveSearch)
    local bundleInp = make("TextBox", {Size = UDim2.new(0.65, -5, 1, 0), BackgroundColor3 = Color3.fromRGB(20, 20, 26), PlaceholderText = "Англ. название (напр: Ninja, Superhero)...", Text = "", TextColor3 = Color3.fromRGB(255, 255, 255), Font = Enum.Font.Gotham, TextSize = 12}, bundleSearchRow) addCorner(bundleInp, 6)
    local bundleBtn = make("TextButton", {Size = UDim2.new(0.35, 0, 1, 0), Position = UDim2.new(0.65, 5, 0, 0), BackgroundColor3 = Color3.fromRGB(150, 50, 200), Text = "Найти Пак", TextColor3 = Color3.fromRGB(255, 255, 255), Font = Enum.Font.GothamBold, TextSize = 12}, bundleSearchRow) addCorner(bundleBtn, 6)

    local bundleResultsContainer = make("Frame", {Size = UDim2.new(1, -10, 0, 0), AutomaticSize = Enum.AutomaticSize.Y, BackgroundTransparency = 1, LayoutOrder = 2}, vMoveSearch)
    make("UIListLayout", {SortOrder = Enum.SortOrder.LayoutOrder, Padding = UDim.new(0, 5)}, bundleResultsContainer)

    local function renderMoveUI() end 
    
    local function createBundleSearchRow(bundleId, bundleName)
        local Row = make("Frame", {Size = UDim2.new(1, 0, 0, 44), BackgroundColor3 = Color3.fromRGB(25, 25, 32)}, bundleResultsContainer) addCorner(Row, 6)
        local Icon = make("ImageLabel", {Size = UDim2.new(0, 36, 0, 36), Position = UDim2.new(0, 4, 0.5, -18), BackgroundColor3 = Color3.fromRGB(40,40,45), BackgroundTransparency = 0, Image = "rbxthumb://type=Bundle&id=" .. tostring(bundleId) .. "&w=150&h=150"}, Row) addCorner(Icon, 6)
        local nameLbl = make("TextLabel", {Size = UDim2.new(0.55, -45, 1, 0), Position = UDim2.new(0, 45, 0, 0), BackgroundTransparency=1, TextColor3=Color3.fromRGB(255,255,255), Text=bundleName, Font=Enum.Font.Gotham, TextSize=11, TextXAlignment=Enum.TextXAlignment.Left, TextTruncate=Enum.TextTruncate.AtEnd}, Row)
        
        local applyBtn = make("TextButton", {Size=UDim2.new(0.2, 0, 0, 28), Position=UDim2.new(0.55, 0, 0.5, -14), BackgroundColor3=Color3.fromRGB(40,160,60), Text="✨ Прим.", TextColor3=Color3.fromRGB(255,255,255), Font=Enum.Font.GothamBold, TextSize=10}, Row) addCorner(applyBtn, 4)
        local saveCatBtn = make("TextButton", {Size=UDim2.new(0.2, 0, 0, 28), Position=UDim2.new(0.78, 0, 0.5, -14), BackgroundColor3=Color3.fromRGB(40,120,200), Text="➕ Сохр.", TextColor3=Color3.fromRGB(255,255,255), Font=Enum.Font.GothamBold, TextSize=10}, Row) addCorner(saveCatBtn, 4)

        applyBtn.MouseButton1Click:Connect(function() applyBundle(bundleId, bundleName) end)
        saveCatBtn.MouseButton1Click:Connect(function() 
            local exists = false
            for _, v in ipairs(customPacks) do if v.id == tostring(bundleId) then exists = true break end end
            if not exists then 
                table.insert(customPacks, {name = bundleName, id = tostring(bundleId)}) 
                saveData() renderMoveUI() showNotice("Пак сохранен!") 
            else showNotice("Уже в списке!") end
        end)
    end

    bundleBtn.MouseButton1Click:Connect(function()
        for _, child in ipairs(bundleResultsContainer:GetChildren()) do if child:IsA("Frame") then child:Destroy() end end
        local q = bundleInp.Text if q == "" then return end
        showNotice("Ищем паки анимаций...")
        task.spawn(function()
            local s2, r2 = pcall(function()
                local searchParams = CatalogSearchParams.new()
                searchParams.SearchKeyword = q
                searchParams.BundleTypes = {Enum.BundleType.Animations}
                searchParams.SortType = Enum.CatalogSortType.Relevance
                return AvatarEditorService:SearchCatalog(searchParams)
            end)
            if s2 and r2 then
                local items = r2:GetCurrentPage()
                if #items > 0 then
                    showNotice("Найдено результатов: " .. #items)
                    for _, item in ipairs(items) do createBundleSearchRow(item.Id, item.Name) end
                else showNotice("Паки по этому запросу не найдены!") end
            else showNotice("Сбой поиска!") end
        end)
    end)

    make("UIListLayout", {SortOrder = Enum.SortOrder.LayoutOrder, Padding = UDim.new(0, 8)}, vMoveSaved)
    CreateButton(vMoveSaved, "Сбросить анимацию (Ресет)", function() mySetts.selectedPackData = nil saveData() showNotice("Сброшено! Ресетнись.") end, Color3.fromRGB(180, 50, 50), 1)

    local packsContainer = make("Frame", {Size = UDim2.new(1,-10,0,0), AutomaticSize = Enum.AutomaticSize.Y, BackgroundTransparency = 1, LayoutOrder = 2}, vMoveSaved)
    make("UIListLayout", {SortOrder = Enum.SortOrder.LayoutOrder, Padding = UDim.new(0, 5)}, packsContainer)

    local function createPackRow(name, bundleId, manualData)
        local Row = make("Frame", {Size = UDim2.new(1, 0, 0, 35), BackgroundTransparency = 1}, packsContainer)
        local PlayBtn = make("TextButton", {Size = UDim2.new(1, -45, 1, 0), BackgroundColor3 = Color3.fromRGB(40, 40, 48), Text = "⭐ " .. name, TextColor3 = Color3.fromRGB(220, 220, 220), Font = Enum.Font.GothamSemibold, TextSize = 13}, Row) addCorner(PlayBtn, 6)

        if manualData then
            PlayBtn.MouseButton1Click:Connect(function()
                mySetts.selectedPackData = manualData
                saveData() applyMove(unpack(manualData)) showNotice("Пак применен!")
            end)
        else
            PlayBtn.MouseButton1Click:Connect(function() applyBundle(tonumber(bundleId), name) end)
        end

        local DelBtn = make("TextButton", {Size = UDim2.new(0, 40, 1, 0), Position = UDim2.new(1, -40, 0, 0), BackgroundColor3 = Color3.fromRGB(180, 50, 50), Text = "🗑️", TextColor3 = Color3.fromRGB(255, 255, 255), Font = Enum.Font.GothamBold, TextSize = 12}, Row) addCorner(DelBtn, 6)
        DelBtn.MouseButton1Click:Connect(function() 
            for i, v in ipairs(customPacks) do if v.id == bundleId then table.remove(customPacks, i) break end end 
            saveData() Row:Destroy()
        end)
    end

    renderMoveUI = function()
        for _, child in ipairs(packsContainer:GetChildren()) do if child:IsA("Frame") then child:Destroy() end end
        for _, v in ipairs(customPacks) do createPackRow(v.name, v.id, v.isManual and v.data or nil) end
    end
    renderMoveUI()

    CreateLabel(vMoveSaved, "Свои ID движений (Ручной ввод / Сохранение)", 3)
    local mName = CreateTextBox(vMoveSaved, "Название для сохранения пака...", "", 4)
    local mI = CreateTextBox(vMoveSaved, "Idle ID", "", 5)
    local mW = CreateTextBox(vMoveSaved, "Walk ID", "", 6)
    local mR = CreateTextBox(vMoveSaved, "Run ID", "", 7)
    local mJ = CreateTextBox(vMoveSaved, "Jump ID", "", 8)
    local mF = CreateTextBox(vMoveSaved, "Fall ID", "", 9)

    local manualActionRow = make("Frame", {Size = UDim2.new(1, -10, 0, 35), BackgroundTransparency = 1, LayoutOrder = 10}, vMoveSaved)
    local btnApplyMan = make("TextButton", {Size = UDim2.new(0.48, 0, 1, 0), BackgroundColor3 = Color3.fromRGB(40, 160, 60), Text = "▶ Применить без сохр.", TextColor3 = Color3.fromRGB(255, 255, 255), Font = Enum.Font.GothamBold, TextSize = 11}, manualActionRow) addCorner(btnApplyMan, 6)
    local btnSaveMan = make("TextButton", {Size = UDim2.new(0.48, 0, 1, 0), Position = UDim2.new(0.52, 0, 0, 0), BackgroundColor3 = Color3.fromRGB(150, 50, 200), Text = "➕ Сохранить Пак", TextColor3 = Color3.fromRGB(255, 255, 255), Font = Enum.Font.GothamBold, TextSize = 11}, manualActionRow) addCorner(btnSaveMan, 6)

    btnApplyMan.MouseButton1Click:Connect(function()
        mySetts.selectedPackData = {mI.Text, mW.Text, mR.Text, mJ.Text, mF.Text} saveData() applyMove(unpack(mySetts.selectedPackData)) showNotice("Применено!")
    end)
    
    btnSaveMan.MouseButton1Click:Connect(function()
        local n = mName.Text
        if n == "" then n = "My Custom Pack" end
        local mData = {mI.Text, mW.Text, mR.Text, mJ.Text, mF.Text}
        local uniqueId = "manual_" .. tostring(math.random(10000, 99999))
        table.insert(customPacks, {name = n, id = uniqueId, isManual = true, data = mData})
        saveData() renderMoveUI() showNotice("Пак " .. n .. " сохранен!")
        mName.Text = "" mI.Text = "" mW.Text = "" mR.Text = "" mJ.Text = "" mF.Text = ""
    end)

    if player.Character and player.Character:FindFirstChild("Humanoid") then
        if mySetts.selectedPackData then
            task.delay(0.5, function() applyMove(unpack(mySetts.selectedPackData)) end)
        end
    end

    player.CharacterAdded:Connect(function(char)
        local h = char:WaitForChild("Humanoid", 5)
        if h then
            if mySetts.autoSpeed then h.WalkSpeed = mySetts.targetSpeed end
            if mySetts.autoJump then h.UseJumpPower = true h.JumpPower = mySetts.targetJump end
            if mySetts.selectedPackData then task.delay(1, function() applyMove(unpack(mySetts.selectedPackData)) end) end
            if mySetts.autoPlaySong and mySetts.lastPlayedSongId then
                task.delay(2.5, function() if char:FindFirstChild("Humanoid") and char.Humanoid.Health > 0 then pcall(function() game.ReplicatedStorage.Remotes.Inventory.PlaySong:FireServer("https://www.roblox.com/asset/?id=" .. mySetts.lastPlayedSongId) end) end end)
            end
            if isFastResetting and fastResetCFrame then
                task.delay(0.3, function() local hrp = char:WaitForChild("HumanoidRootPart", 3) if hrp then hrp.CFrame = fastResetCFrame isFastResetting = false fastResetCFrame = nil end end)
            end
        end
    end)
end

-- ================== 3. MM2 ЧИТЫ И ТРЕКИ ==================
do
    CreateLabel(vMm2, "Маньяк и Шериф (ТП и Моды)")
    
    local mm2Tps = make("Frame", {Size = UDim2.new(1, -10, 0, 35), BackgroundTransparency = 1}, vMm2)
    local btnM = make("TextButton", {Size = UDim2.new(0.48, 0, 1, 0), BackgroundColor3 = Color3.fromRGB(180, 50, 50), Text = "ТП к Маньяку", TextColor3 = Color3.fromRGB(255,255,255), Font = Enum.Font.GothamBold, TextSize = 12}, mm2Tps) addCorner(btnM, 6)
    local btnS = make("TextButton", {Size = UDim2.new(0.48, 0, 1, 0), Position = UDim2.new(0.52, 0, 0, 0), BackgroundColor3 = Color3.fromRGB(50, 100, 180), Text = "ТП к Шерифу", TextColor3 = Color3.fromRGB(255,255,255), Font = Enum.Font.GothamBold, TextSize = 12}, mm2Tps) addCorner(btnS, 6)

    btnM.MouseButton1Click:Connect(function()
        local t = nil
        for _, p in ipairs(Players:GetPlayers()) do
            if p ~= player and p.Character and p.Character:FindFirstChild("HumanoidRootPart") and p.Character:FindFirstChild("Humanoid") and p.Character.Humanoid.Health > 0 then
                local r, _ = getRoleInfo(p) if r == "MURDERER" then t = p.Character break end
            end
        end
        if t and t:FindFirstChild("HumanoidRootPart") and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
            player.Character.HumanoidRootPart.CFrame = t.HumanoidRootPart.CFrame * CFrame.new(0, 0, 3) showNotice("Телепортировано к Маньяку!")
        else showNotice("⚠️ Маньяк не найден!") end
    end)
    btnS.MouseButton1Click:Connect(function()
        local t = nil
        for _, p in ipairs(Players:GetPlayers()) do
            if p ~= player and p.Character and p.Character:FindFirstChild("HumanoidRootPart") and p.Character:FindFirstChild("Humanoid") and p.Character.Humanoid.Health > 0 then
                local r, _ = getRoleInfo(p) if r == "SHERIFF" then t = p.Character break end
            end
        end
        if t and t:FindFirstChild("HumanoidRootPart") and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
            player.Character.HumanoidRootPart.CFrame = t.HumanoidRootPart.CFrame * CFrame.new(0, 0, 3) showNotice("Телепортировано к Шерифу!")
        else showNotice("⚠️ Шериф не найден!") end
    end)

    CreateToggleWithBind(vMm2, "🎯 Auto Shoot (Если ты Шериф)", "MM2_AutoShoot", false, function(state)
        isAutoShoot = state
    end)

    CreateToggleWithBind(vMm2, "🎯 Silent Aim (Хитбокс / Магнит)", "MM2_SilentAim", false, function(state)
        isSilentAim = state
    end)

    CreateLabel(vMm2, "Визуал ESP и Полезное")
    
    CreateToggleWithBind(vMm2, "👁️ X-Ray (Wallhack на карту)", "MM2_XRay", false, function(state)
        isXRay = state
        if isXRay then
            for _, v in ipairs(workspace:GetDescendants()) do
                if v:IsA("BasePart") and not v.Parent:FindFirstChild("Humanoid") then
                    if origXrayTrans[v] == nil then origXrayTrans[v] = v.Transparency end
                    v.Transparency = 0.75
                end
            end
        else
            for v, t in pairs(origXrayTrans) do
                if v and v.Parent then v.Transparency = t end
            end
            origXrayTrans = {}
        end
    end)

    workspace.DescendantAdded:Connect(function(v)
        if isXRay and v:IsA("BasePart") and not v.Parent:FindFirstChild("Humanoid") then
            origXrayTrans[v] = v.Transparency
            v.Transparency = 0.75
        end
    end)

    CreateToggleWithBind(vMm2, "Role ESP", false, false, function(state)
        isRoleEsp = state
        if not isRoleEsp then 
            for _, p in ipairs(Players:GetPlayers()) do
                if p.Character then
                    local hl = p.Character:FindFirstChild("KutomRoleHL") if hl then hl:Destroy() end
                    local hd = p.Character:FindFirstChild("Head") if hd and hd:FindFirstChild("KutomRoleBG") then hd.KutomRoleBG:Destroy() end
                end
            end
        end 
    end)

    CreateToggleWithBind(vMm2, "Gun Drop ESP", false, false, function(state)
        isGunEsp = state 
        if not isGunEsp then 
            for _, d in ipairs(workspace:GetDescendants()) do if d.Name == "KutomGunHL" or d.Name == "KutomGunBG" then d:Destroy() end end
        end 
    end)

    CreateToggleWithBind(vMm2, "Автоподбор пистолета", false, false, function(state) isAutoGrab = state end)

    CreateLabel(vMm2, "Визуал Прицела")
    local isCrosshair = false
    local customCrosshair = make("Frame", {Size = UDim2.new(0, 6, 0, 6), AnchorPoint = Vector2.new(0.5, 0.5), BackgroundColor3 = Color3.fromRGB(255, 255, 255), ZIndex = 100000, Visible = false}, sgui) addCorner(customCrosshair, 4) addStroke(customCrosshair, Color3.fromRGB(0, 0, 0), 1)

    CreateToggleWithBind(vMm2, "Радужный прицел", false, false, function(state)
        isCrosshair = state
        if not isCrosshair then UIS.MouseIconEnabled = true customCrosshair.Visible = false end
    end)

    CreateLabel(vMm2, "Менеджер Треков (Радио)")
    local mm2TrackScroll = make("ScrollingFrame", {Size = UDim2.new(1, -10, 0, 180), BackgroundColor3 = Color3.fromRGB(20, 20, 26), BorderSizePixel = 0, ScrollBarThickness = 3, AutomaticCanvasSize = Enum.AutomaticSize.Y, CanvasSize = UDim2.new(0,0,0,0)}, vMm2)
    addCorner(mm2TrackScroll, 6) make("UIListLayout", {Padding = UDim.new(0, 4), SortOrder = Enum.SortOrder.LayoutOrder}, mm2TrackScroll)

    local function renderMm2Tracks()
        for _, child in ipairs(mm2TrackScroll:GetChildren()) do if child:IsA("Frame") then child:Destroy() end end
        for i, track in ipairs(mm2Tracks) do
            local trFrame = make("Frame", {Size = UDim2.new(1, -10, 0, 32), BackgroundTransparency = 1}, mm2TrackScroll)
            local playBtn = make("TextButton", {Size = UDim2.new(1, -45, 1, 0), BackgroundColor3 = Color3.fromRGB(40, 120, 200), Text = "🎵 " .. track.name, TextColor3 = Color3.fromRGB(255, 255, 255), Font = Enum.Font.GothamBold, TextSize = 12}, trFrame) addCorner(playBtn, 4)
            local delBtn = make("TextButton", {Size = UDim2.new(0, 40, 1, 0), Position = UDim2.new(1, -40, 0, 0), BackgroundColor3 = Color3.fromRGB(180, 50, 50), Text = "🗑️", TextColor3 = Color3.fromRGB(255, 255, 255), Font = Enum.Font.GothamBold, TextSize = 12}, trFrame) addCorner(delBtn, 4)
            
            playBtn.MouseButton1Click:Connect(function()
                local char = player.Character local hum = char and char:FindFirstChildOfClass("Humanoid")
                if not char or not hum or hum.Health <= 0 then showNotice("⚠️ Вы находитесь на спавне!") return end
                mySetts.lastPlayedSongId = track.id saveData() pcall(function() game.ReplicatedStorage.Remotes.Inventory.PlaySong:FireServer("https://www.roblox.com/asset/?id=" .. track.id) showNotice("▶ Играет: " .. track.name) end)
            end)
            delBtn.MouseButton1Click:Connect(function() table.remove(mm2Tracks, i) saveData() renderMm2Tracks() showNotice("Трек удален!") end)
        end
    end
    renderMm2Tracks()

    local trNameInp = CreateTextBox(vMm2, "Название (пусто = авто)")
    local trIdInp = CreateTextBox(vMm2, "Sound ID трека...")
    CreateButton(vMm2, "➕ Добавить / Переименовать Трек", function()
        local rawId = string.match(trIdInp.Text, "%d+")
        if rawId then
            local customName = trNameInp.Text local finalName = ""
            local success, info = pcall(function() return game:GetService("MarketplaceService"):GetProductInfo(tonumber(rawId)) end)
            local realName = (success and info) and info.Name or nil
            if customName ~= "" and realName then finalName = customName .. " (" .. realName .. ")" elseif customName == "" and realName then finalName = realName elseif customName == "" and not realName then finalName = "Audio_" .. rawId else finalName = customName end
            
            local exists = false
            for _, t in ipairs(mm2Tracks) do
                if t.id == rawId then t.name = finalName exists = true break end
            end
            if not exists then table.insert(mm2Tracks, {name = finalName, id = rawId}) end
            saveData() renderMm2Tracks() trNameInp.Text = "" trIdInp.Text = "" showNotice(exists and "Трек переименован!" or "Трек добавлен!")
        else showNotice("⚠️ Введите корректный Sound ID!") end
    end, Color3.fromRGB(40, 160, 60))
    CreateToggleWithBind(vMm2, "Авто-плей трека после смерти", false, mySetts.autoPlaySong, function(state) mySetts.autoPlaySong = state saveData() end)

    task.spawn(function()
        while task.wait(1) do
            pcall(function()
                local remotes = game:GetService("ReplicatedStorage"):FindFirstChild("Remotes")
                if remotes and remotes:FindFirstChild("Extras") and remotes.Extras:FindFirstChild("GetPlayerData") then
                    local data = remotes.Extras.GetPlayerData:InvokeServer()
                    if data then mm2EarlyRoles = {} for plrName, pData in pairs(data) do if pData.Role then mm2EarlyRoles[plrName] = pData.Role end end end
                end
            end)
        end
    end)

    RS.RenderStepped:Connect(function()
        pcall(function()
            if isCrosshair then
                UIS.MouseIconEnabled = false
                customCrosshair.Visible = true
                local msLoc = UIS:GetMouseLocation()
                customCrosshair.Position = UDim2.new(0, msLoc.X, 0, msLoc.Y)
                customCrosshair.BackgroundColor3 = Color3.fromHSV((tick() % 2) / 2, 1, 1)
            end
            
            if isFovEnabled then
                workspace.CurrentCamera.FieldOfView = customFov
            end
        end)
    end)

    task.spawn(function()
        while task.wait(0.05) do
            pcall(function()
                if isAutoShoot and player.Character then
                    local myGun = player.Character:FindFirstChild("Gun") or player.Character:FindFirstChild("Revolver")
                    if myGun then
                        local murd = nil
                        for _, p in ipairs(Players:GetPlayers()) do
                            if p ~= player and p.Character and p.Character:FindFirstChild("Humanoid") and p.Character.Humanoid.Health > 0 then
                                local r = getRoleInfo(p)
                                if r == "MURDERER" then murd = p.Character break end
                            end
                        end
                        if murd and murd:FindFirstChild("HumanoidRootPart") then
                            player.Character.HumanoidRootPart.CFrame = CFrame.lookAt(player.Character.HumanoidRootPart.Position, murd.HumanoidRootPart.Position)
                            local shootRemote = myGun:FindFirstChild("KnifeServer") and myGun.KnifeServer:FindFirstChild("ShootGun")
                            if shootRemote then
                                shootRemote:InvokeServer(1, murd.HumanoidRootPart.Position, "AH")
                            end
                        end
                    end
                end
                
                if isAutoEvade and player.Character and player.Character:FindFirstChild("HumanoidRootPart") and player.Character:FindFirstChild("Humanoid") and player.Character.Humanoid.Health > 0 then
                    local murd = nil
                    for _, p in ipairs(Players:GetPlayers()) do
                        if p ~= player and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                            if getRoleInfo(p) == "MURDERER" then
                                murd = p.Character
                                break
                            end
                        end
                    end
                    
                    if murd and murd:FindFirstChild("HumanoidRootPart") then
                        local myPos = player.Character.HumanoidRootPart.Position
                        local murdPos = murd.HumanoidRootPart.Position
                        local dist = (myPos - murdPos).Magnitude
                        
                        -- Радиус увеличен до 30 стадов для безопасности
                        if dist < 30 then
                            local dir = (myPos - murdPos).Unit
                            dir = Vector3.new(dir.X, 0, dir.Z).Unit -- Убираем Y, чтобы бежать ровно по земле
                            
                            -- Задаем точку, в которую нужно убегать (на 15 стадов дальше от маньяка)
                            local safePoint = myPos + (dir * 15)
                            player.Character.Humanoid:MoveTo(safePoint)
                            
                            -- Экстренный авто-прыжок, если маньяк почти достал (12 стадов)
                            if dist < 12 and player.Character.Humanoid.FloorMaterial ~= Enum.Material.Air then
                                player.Character.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
                            end
                        end
                    end
                end

                if isRoleEsp then
                    for _, p in ipairs(game.Players:GetPlayers()) do
                        if p ~= player and p.Character then
                            local char = p.Character
                            local hum = char:FindFirstChild("Humanoid")
                            if hum and hum.Health > 0 then
                                local role, col = getRoleInfo(p)
                                local hl = char:FindFirstChild("KutomRoleHL")
                                if not hl then
                                    hl = Instance.new("Highlight")
                                    hl.Name = "KutomRoleHL"
                                    hl.FillTransparency = 0.6
                                    hl.Parent = char
                                end
                                hl.FillColor = col
                                hl.OutlineColor = Color3.new(1,1,1)
                                local hd = char:FindFirstChild("Head")
                                if hd then
                                    local bg = hd:FindFirstChild("KutomRoleBG")
                                    if not bg then
                                        bg = Instance.new("BillboardGui")
                                        bg.Name = "KutomRoleBG"
                                        bg.Size = UDim2.new(0, 100, 0, 40)
                                        bg.StudsOffset = Vector3.new(0, 2, 0)
                                        bg.AlwaysOnTop = true
                                        bg.Parent = hd
                                        local tl = Instance.new("TextLabel")
                                        tl.Name = "TL"
                                        tl.Size = UDim2.new(1,0,1,0)
                                        tl.BackgroundTransparency = 1
                                        tl.TextStrokeTransparency = 0
                                        tl.Font = Enum.Font.GothamBold
                                        tl.TextSize = 14
                                        tl.Parent = bg
                                    end
                                    local tl = bg:FindFirstChild("TL")
                                    if tl then
                                        tl.Text = p.Name .. "\n[" .. role .. "]"
                                        tl.TextColor3 = col
                                    end
                                end
                            end
                        end
                    end
                end

                local gunDrop = workspace:FindFirstChild("GunDrop", true)
                if isGunEsp or isAutoGrab then
                    if gunDrop then
                        local targetPart = gunDrop:IsA("BasePart") and gunDrop or (gunDrop:IsA("Model") and gunDrop.PrimaryPart or gunDrop:FindFirstChildWhichIsA("BasePart"))
                        if targetPart then
                            if isGunEsp then
                                local hl = targetPart:FindFirstChild("KutomGunHL")
                                if not hl then
                                    hl = Instance.new("Highlight")
                                    hl.Name = "KutomGunHL"
                                    hl.FillColor = Color3.fromRGB(255, 255, 0)
                                    hl.FillTransparency = 0.2
                                    hl.Parent = targetPart
                                end
                                local bg = targetPart:FindFirstChild("KutomGunBG")
                                if not bg then
                                    bg = Instance.new("BillboardGui")
                                    bg.Name = "KutomGunBG"
                                    bg.Size = UDim2.new(0, 150, 0, 30)
                                    bg.AlwaysOnTop = true
                                    bg.Parent = targetPart
                                    local tl = Instance.new("TextLabel")
                                    tl.Size = UDim2.new(1,0,1,0)
                                    tl.BackgroundTransparency = 1
                                    tl.Text = "🔫 ПИСТОЛЕТ ЗДЕСЬ!"
                                    tl.TextColor3 = Color3.fromRGB(255, 255, 0)
                                    tl.TextStrokeTransparency = 0
                                    tl.Font = Enum.Font.GothamBold
                                    tl.TextSize = 14
                                    tl.Parent = bg
                                end
                            end
                            if isAutoGrab and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                                local hrp = player.Character.HumanoidRootPart
                                if firetouchinterest then 
                                    firetouchinterest(hrp, targetPart, 0) 
                                    task.wait() 
                                    firetouchinterest(hrp, targetPart, 1) 
                                else 
                                    hrp.CFrame = targetPart.CFrame 
                                end
                            end
                        end
                    end
                end
            end)
        end
    end)
end

-- ================== 4. VIP-МЕНЮ ==================
do
    CreateLabel(vVip, "Kutom Fling Script")
    CreateButton(vVip, "🌪️ Запустить KutomFling", function()
        pcall(function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/Egorzq/KutomHub/refs/heads/main/Simple-KutomFling"))()
            showNotice("KutomFling запущен!")
        end)
    end, Color3.fromRGB(150, 50, 200))

    CreateLabel(vVip, "Статы Игрока")
    CreateActionRow(vVip, "Скорость (16)", "Применить Скор.", function(val)
        local n = tonumber(val) if n and player.Character and player.Character:FindFirstChildOfClass("Humanoid") then
            player.Character:FindFirstChildOfClass("Humanoid").WalkSpeed = n mySetts.targetSpeed = n saveData() showNotice("Скорость: " .. n)
        end
    end, "Сброс", function()
        mySetts.targetSpeed = 16 saveData() if player.Character and player.Character:FindFirstChildOfClass("Humanoid") then player.Character:FindFirstChildOfClass("Humanoid").WalkSpeed = 16 end
    end)

    CreateActionRow(vVip, "Прыжок (50)", "Применить Прыжок", function(val)
        local n = tonumber(val) if n and player.Character and player.Character:FindFirstChildOfClass("Humanoid") then
            player.Character:FindFirstChildOfClass("Humanoid").UseJumpPower = true
            player.Character:FindFirstChildOfClass("Humanoid").JumpPower = n mySetts.targetJump = n saveData() showNotice("Прыжок: " .. n)
        end
    end, "Сброс", function()
        mySetts.targetJump = 50 saveData() if player.Character and player.Character:FindFirstChildOfClass("Humanoid") then player.Character:FindFirstChildOfClass("Humanoid").UseJumpPower = true player.Character:FindFirstChildOfClass("Humanoid").JumpPower = 50 end
    end)

    CreateLabel(vVip, "Чит-моды", 4)

    local flyBindCont = make("Frame", {Size = UDim2.new(1, -10, 0, 34), BackgroundTransparency = 1, LayoutOrder = 5}, vVip)
    local flyBtnObj = make("TextButton", {Size = UDim2.new(1, -50, 1, 0), BackgroundColor3 = Color3.fromRGB(40, 120, 200), TextColor3 = Color3.fromRGB(255, 255, 255), Text = "Полет", Font = Enum.Font.GothamBold, TextSize = 12}, flyBindCont) addCorner(flyBtnObj, 6)
    local flyBindBtn = make("TextButton", {Size = UDim2.new(0, 45, 1, 0), Position = UDim2.new(1, -45, 0, 0), BackgroundColor3 = Color3.fromRGB(35, 35, 45), Font = Enum.Font.GothamBold, TextSize = 11}, flyBindCont) addCorner(flyBindBtn, 6)
    
    setupBind(flyBindBtn, "VIP_Fly")

    flyBtnObj.MouseButton1Click:Connect(function()
        toggleFly()
        if isFly then
            flyBtnObj.Text = "Выкл Полет" tween(flyBtnObj, 0.2, {BackgroundColor3 = Color3.fromRGB(180, 40, 40)})
        else 
            flyBtnObj.Text = "Полет" tween(flyBtnObj, 0.2, {BackgroundColor3 = Color3.fromRGB(40, 120, 200)}) 
        end
    end)
    
    CreateActionRow(vVip, "Скор. полета (50)", "Применить", function(val)
        local n = tonumber(val)
        if n then
            mySetts.flySpeed = n
            saveData()
            showNotice("Скор. полета: " .. n)
        end
    end, "Сброс", function()
        mySetts.flySpeed = 50
        saveData()
        showNotice("Сброшено до 50")
    end)

    local noclipConn
    CreateToggleWithBind(vVip, "Noclip", false, false, function(state)
        if state then
            noclipConn = RS.Stepped:Connect(function()
                if player.Character then
                    for _, v in ipairs(player.Character:GetDescendants()) do if v:IsA("BasePart") and v.CanCollide then v.CanCollide = false end end
                end
            end)
        else
            if noclipConn then noclipConn:Disconnect() noclipConn = nil end
        end
    end, 6)

    local isInfJump = false
    CreateToggleWithBind(vVip, "Бесконечный прыжок", false, false, function(state) isInfJump = state end, 7)
    UIS.JumpRequest:Connect(function() 
        if isInfJump and player.Character then local h = player.Character:FindFirstChildOfClass("Humanoid") if h then h:ChangeState(Enum.HumanoidStateType.Jumping) end end 
    end)

    CreateToggleWithBind(vVip, "🛡️ Anti-Fling (Защита от флинга)", "VIP_AntiFling", false, function(state) isAntiFling = state end, 9)
    RS.Stepped:Connect(function()
        if isAntiFling then
            pcall(function()
                for _, p in ipairs(game.Players:GetPlayers()) do
                    if p ~= player and p.Character then
                        for _, v in ipairs(p.Character:GetChildren()) do if v:IsA("BasePart") then v.CanCollide = false end end
                    end
                end
                local char = player.Character
                if char and char.Parent then
                    local hrp = char:FindFirstChild("HumanoidRootPart")
                    if hrp and (hrp.Velocity.Magnitude > 250 or hrp.RotVelocity.Magnitude > 250) then
                        hrp.Velocity = Vector3.zero hrp.RotVelocity = Vector3.zero
                    end
                end
            end)
        end
    end)

    CreateLabel(vVip, "Взаимодействие с Игроками", 12)
    CreateActionRow(vVip, "Ник игрока...", "Телепорт к Игроку", function(val)
        local tName = val:lower() local t = nil
        for _, p in ipairs(Players:GetPlayers()) do
            if p ~= player and (string.sub(p.Name:lower(), 1, #tName) == tName or string.sub(p.DisplayName:lower(), 1, #tName) == tName) then t = p break end
        end
        if t and t.Character and t.Character:FindFirstChild("HumanoidRootPart") and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
            player.Character.HumanoidRootPart.CFrame = t.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, 3) showNotice("Телепорт к " .. t.Name)
        else showNotice("⚠️ Игрок не найден!") end
    end)
end

-- ================== 5. TOOLS (ИНСТРУМЕНТЫ) ==================
do
    CreateLabel(vTools, "Выдача Инструментов", 1)

    CreateButton(vTools, "🖱️ Выдать Click TP", function()
        if player.Backpack:FindFirstChild("Click TP") or (player.Character and player.Character:FindFirstChild("Click TP")) then showNotice("Уже в инвентаре!") return end
        local tpTool = Instance.new("Tool") tpTool.Name = "Click TP" tpTool.RequiresHandle = false tpTool.CanBeDropped = false
        tpTool.Activated:Connect(function()
            if mouse.Target and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then player.Character.HumanoidRootPart.CFrame = CFrame.new(mouse.Hit.p + Vector3.new(0, 3, 0)) end
        end)
        tpTool.Parent = player.Backpack showNotice("Click TP выдан в инвентарь!")
    end, Color3.fromRGB(25, 25, 32), 2)

    CreateLabel(vTools, "Настройки Gold Part", 3)
    CreateActionRow(vTools, "Время в сек (умолч. 3)", "Применить", function(val)
        local n = tonumber(val)
        if n and n > 0 then
            mySetts.goldPartDuration = n
            saveData()
            showNotice("Gold Part будет жить: " .. n .. " сек")
        end
    end, "Сброс", function()
        mySetts.goldPartDuration = 3
        saveData()
        showNotice("Сброшено до 3 сек")
    end, 4)

    local isGoldPartJump = true
    CreateToggleWithBind(vTools, "⬆️ Авто-прыжок при установке Gold Part", "Tools_GoldJump", true, function(state)
        isGoldPartJump = state
    end, 5)

    CreateButton(vTools, "🟨 Выдать Gold Part", function()
        if player.Backpack:FindFirstChild("Gold Part") or (player.Character and player.Character:FindFirstChild("Gold Part")) then showNotice("Уже в инвентаре!") return end
        local tool = Instance.new("Tool")
        tool.Name = "Gold Part"
        tool.RequiresHandle = false
        tool.CanBeDropped = false
        tool.Activated:Connect(function()
            local char = player.Character
            local hrp = char and char:FindFirstChild("HumanoidRootPart")
            if hrp then
                local part = Instance.new("Part")
                part.Name = "KutomGoldPart"
                part.Size = Vector3.new(4, 1, 4)
                part.Anchored = true
                part.BrickColor = BrickColor.new("Bright yellow")
                part.Material = Enum.Material.Neon
                part.CFrame = hrp.CFrame * CFrame.new(0, -3.5, 0)
                part.Parent = workspace
                game:GetService("Debris"):AddItem(part, mySetts.goldPartDuration or 3) 
                
                local hum = char:FindFirstChildOfClass("Humanoid")
                if hum and isGoldPartJump then
                    hum:ChangeState(Enum.HumanoidStateType.Jumping)
                end
            end
        end)
        tool.Parent = player.Backpack
        showNotice("Gold Part выдан в инвентарь!")
    end, Color3.fromRGB(200, 150, 0), 6)

    CreateButton(vTools, "🧹 Удалить все Gold Parts", function()
        local count = 0
        for _, v in ipairs(workspace:GetChildren()) do
            if v.Name == "KutomGoldPart" then
                v:Destroy()
                count = count + 1
            end
        end
        showNotice("Удалено партов: " .. count)
    end, Color3.fromRGB(180, 50, 50), 7)
end

-- ================== 6. ПРОЧЕЕ ==================
do
    CreateLabel(vOther, "Утилиты для выживания", 0)
    
    CreateToggleWithBind(vOther, "🏃 Auto-Evade (Убегать от маньяка)", "Other_AutoEvade", false, function(state)
        isAutoEvade = state
    end, 1)

    local fResetCont = make("Frame", {Size = UDim2.new(1, -10, 0, 34), BackgroundTransparency = 1, LayoutOrder = 2}, vOther)
    local fastResetBtn = make("TextButton", {Size = UDim2.new(1, -50, 1, 0), BackgroundColor3 = Color3.fromRGB(180, 50, 50), TextColor3 = Color3.fromRGB(255, 255, 255), Text = "💀 Быстрый Ресет (Возврат)", Font = Enum.Font.GothamBold, TextSize = 12}, fResetCont) addCorner(fastResetBtn, 6)
    local fastResetBindBtn = make("TextButton", {Size = UDim2.new(0, 45, 1, 0), Position = UDim2.new(1, -45, 0, 0), BackgroundColor3 = Color3.fromRGB(35, 35, 45), Font = Enum.Font.GothamBold, TextSize = 11}, fResetCont) addCorner(fastResetBindBtn, 6)
    
    setupBind(fastResetBindBtn, "Other_FastReset")

    doFastReset = function()
        pcall(function()
            local char = player.Character
            if char and char:FindFirstChild("HumanoidRootPart") and char:FindFirstChild("Humanoid") and char.Humanoid.Health > 0 then
                fastResetCFrame = char.HumanoidRootPart.CFrame
                isFastResetting = true char.Humanoid.Health = 0 showNotice("Ресет... Возвращаем обратно!")
            end
        end)
    end
    fastResetBtn.MouseButton1Click:Connect(doFastReset)

    -- BHOP
    CreateLabel(vOther, "Модификаторы Движения", 3)
    local isBhop = false
    local currentBhopSpeed = 16
    local timeOnGround = 0

    local bhopMaxInp = CreateTextBox(vOther, "Макс. скорость Bhop (по ум. 100)", tostring(mySetts.bhopMaxSpeed), 4)
    bhopMaxInp.FocusLost:Connect(function()
        local n = tonumber(bhopMaxInp.Text)
        if n then
            mySetts.bhopMaxSpeed = n
            saveData()
            showNotice("Макс. скорость Bhop: " .. n)
        end
    end)

    CreateToggleWithBind(vOther, "🐰 Bhop (Ускорение при прыжках)", "Other_Bhop", false, function(state)
        isBhop = state
        if not isBhop and player.Character and player.Character:FindFirstChildOfClass("Humanoid") then
            player.Character:FindFirstChildOfClass("Humanoid").WalkSpeed = mySetts.targetSpeed or 16
        end
    end, 5)

    RS.RenderStepped:Connect(function(dt)
        if isBhop then
            local char = player.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            local hrp = char and char:FindFirstChild("HumanoidRootPart")
            if hum and hrp and hum.Health > 0 then
                local inAir = (hum:GetState() == Enum.HumanoidStateType.Freefall or hum:GetState() == Enum.HumanoidStateType.Jumping)
                local isMoving = hum.MoveDirection.Magnitude > 0
                local baseSpeed = mySetts.targetSpeed or 16
                local maxSpeed = mySetts.bhopMaxSpeed or 100

                if not inAir then
                    timeOnGround = timeOnGround + dt
                    if timeOnGround > 0.3 then
                        currentBhopSpeed = baseSpeed
                    end
                    if isMoving and UIS:IsKeyDown(Enum.KeyCode.Space) then
                        hum:ChangeState(Enum.HumanoidStateType.Jumping)
                        currentBhopSpeed = math.min(maxSpeed, currentBhopSpeed + 8)
                        timeOnGround = 0
                    end
                else
                    timeOnGround = 0
                end
                
                hum.WalkSpeed = currentBhopSpeed

                if isMoving and inAir then
                    local dir = hum.MoveDirection
                    local flatVel = dir * currentBhopSpeed
                    hrp.Velocity = Vector3.new(flatVel.X, hrp.Velocity.Y, flatVel.Z)
                end
            end
        end
    end)

    CreateLabel(vOther, "Телепорты по точкам (Waypoints)", 7)

    local tpContainer = make("Frame", {Size = UDim2.new(1,-10,0,0), AutomaticSize = Enum.AutomaticSize.Y, BackgroundTransparency = 1, LayoutOrder = 8}, vOther)
    make("UIListLayout", {SortOrder = Enum.SortOrder.LayoutOrder, Padding = UDim.new(0, 5)}, tpContainer)

    local function renderTeleports()
        for _, child in ipairs(tpContainer:GetChildren()) do if child:IsA("Frame") then child:Destroy() end end
        for tName, tData in pairs(teleports) do
            local Row = make("Frame", {Size = UDim2.new(1, 0, 0, 35), BackgroundTransparency = 1}, tpContainer)
            local TpBtn = make("TextButton", {Size = UDim2.new(1, -85, 1, 0), BackgroundColor3 = Color3.fromRGB(40, 120, 200), Text = "📍 " .. tName, TextColor3 = Color3.fromRGB(255, 255, 255), Font = Enum.Font.GothamBold, TextSize = 12}, Row) addCorner(TpBtn, 6)
            local BindBtn = make("TextButton", {Size = UDim2.new(0, 35, 1, 0), Position = UDim2.new(1, -80, 0, 0), BackgroundColor3 = Color3.fromRGB(60, 60, 70), Font = Enum.Font.GothamBold, TextSize = 11}, Row) addCorner(BindBtn, 6)
            
            setupBind(BindBtn, "TP_" .. tName)

            local DelBtn = make("TextButton", {Size = UDim2.new(0, 35, 1, 0), Position = UDim2.new(1, -40, 0, 0), BackgroundColor3 = Color3.fromRGB(180, 50, 50), Text = "🗑️", TextColor3 = Color3.fromRGB(255, 255, 255), Font = Enum.Font.GothamBold, TextSize = 12}, Row) addCorner(DelBtn, 6)

            TpBtn.MouseButton1Click:Connect(function()
                tpToPos(CFrame.new(unpack(tData.pos)))
                showNotice("Телепорт на " .. tName)
            end)

            DelBtn.MouseButton1Click:Connect(function()
                teleports[tName] = nil
                binds["TP_" .. tName] = nil
                saveData()
                renderTeleports()
            end)
        end
    end
    renderTeleports()

    local tpSaveRow = make("Frame", {Size = UDim2.new(1, -10, 0, 35), BackgroundTransparency = 1, LayoutOrder = 9}, vOther)
    local tpInp = make("TextBox", {Size = UDim2.new(0.65, -5, 1, 0), BackgroundColor3 = Color3.fromRGB(20, 20, 26), PlaceholderText = "Название точки...", Text = "", TextColor3 = Color3.fromRGB(255, 255, 255), Font = Enum.Font.Gotham, TextSize = 12}, tpSaveRow) addCorner(tpInp, 6)
    local tpAddBtn = make("TextButton", {Size = UDim2.new(0.35, 0, 1, 0), Position = UDim2.new(0.65, 5, 0, 0), BackgroundColor3 = Color3.fromRGB(40, 160, 60), Text = "➕ Сохранить", TextColor3 = Color3.fromRGB(255, 255, 255), Font = Enum.Font.GothamBold, TextSize = 12}, tpSaveRow) addCorner(tpAddBtn, 6)

    tpAddBtn.MouseButton1Click:Connect(function()
        local n = tpInp.Text
        if n ~= "" and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
            local p = player.Character.HumanoidRootPart.Position
            teleports[n] = {pos = {p.X, p.Y, p.Z}}
            saveData()
            renderTeleports()
            tpInp.Text = ""
            showNotice("Точка сохранена!")
        end
    end)

    CreateLabel(vOther, "Маршрутный Тестер (Auto-Walk)", 10)
    local rtPointsContainer = Instance.new("Frame")
    rtPointsContainer.Size = UDim2.new(1, 0, 0, 0)
    rtPointsContainer.AutomaticSize = Enum.AutomaticSize.Y
    rtPointsContainer.BackgroundTransparency = 1
    rtPointsContainer.LayoutOrder = 11
    rtPointsContainer.Parent = vOther
    make("UIListLayout", {Padding = UDim.new(0, 5), SortOrder = Enum.SortOrder.LayoutOrder}, rtPointsContainer)

    local rtPoints = {}
    local rtIsLooping = false
    local rtIsMoving = false
    local rtCurrentTween = nil

    local function parseCoords(str)
        local coords = {}
        for val in string.gmatch(str, "([^,]+)") do table.insert(coords, tonumber(val)) end
        return #coords == 3 and Vector3.new(coords[1], coords[2], coords[3]) or nil
    end

    local function createPointRow()
        local Row = make("Frame", {Size = UDim2.new(1, -10, 0, 34), BackgroundColor3 = Color3.fromRGB(25, 25, 32)}, rtPointsContainer) addCorner(Row, 6)
        local Inp = make("TextBox", {Size = UDim2.new(0.55, -5, 1, 0), Position = UDim2.new(0, 5, 0, 0), BackgroundTransparency=1, TextColor3 = Color3.fromRGB(255,255,255), PlaceholderText = "X, Y, Z", Text = "", Font = Enum.Font.Gotham, TextSize = 12, TextXAlignment = Enum.TextXAlignment.Left}, Row)
        local getBtn = make("TextButton", {Size = UDim2.new(0.2, 0, 0, 24), Position = UDim2.new(0.6, 0, 0.5, -12), BackgroundColor3 = Color3.fromRGB(40, 120, 200), TextColor3 = Color3.fromRGB(255,255,255), Text = "Тут", Font = Enum.Font.GothamBold, TextSize = 11}, Row) addCorner(getBtn, 4)
        local delBtn = make("TextButton", {Size = UDim2.new(0.15, 0, 0, 24), Position = UDim2.new(0.82, 0, 0.5, -12), BackgroundColor3 = Color3.fromRGB(180, 50, 50), TextColor3 = Color3.fromRGB(255,255,255), Text = "X", Font = Enum.Font.GothamBold, TextSize = 11}, Row) addCorner(delBtn, 4)

        local entry = {Input = Inp} table.insert(rtPoints, entry)

        getBtn.MouseButton1Click:Connect(function()
            local c = player.Character if c and c:FindFirstChild("HumanoidRootPart") then Inp.Text = string.format("%.1f, %.1f, %.1f", c.HumanoidRootPart.Position.X, c.HumanoidRootPart.Position.Y, c.HumanoidRootPart.Position.Z) end
        end)
        delBtn.MouseButton1Click:Connect(function()
            for i, v in ipairs(rtPoints) do if v == entry then table.remove(rtPoints, i) break end end Row:Destroy()
        end)
    end
    createPointRow() createPointRow()

    CreateButton(vOther, "+ Добавить точку", createPointRow, Color3.fromRGB(40, 120, 200), 12)
    local rtSpeedInp = CreateTextBox(vOther, "Время пути между точками (сек)", "2", 13)
    CreateToggleWithBind(vOther, "Зациклить маршрут", "Other_LoopRoute", false, function(state) rtIsLooping = state end, 14)

    CreateButton(vOther, "▶ ЗАПУСТИТЬ / СТОП МАРШРУТ", function()
        if rtIsMoving then
            rtIsMoving = false if rtCurrentTween then rtCurrentTween:Cancel() rtCurrentTween = nil end showNotice("Маршрут остановлен!") return
        end
        
        local validPoints = {}
        for _, entry in ipairs(rtPoints) do
            local pos = parseCoords(entry.Input.Text)
            if pos then table.insert(validPoints, pos) end
        end
        
        if #validPoints < 2 then showNotice("⚠️ Нужно минимум 2 точки!") return end
        rtIsMoving = true showNotice("Маршрут запущен!")
        local duration = tonumber(rtSpeedInp.Text) or 2

        task.spawn(function()
            while rtIsMoving do
                for i, targetPos in ipairs(validPoints) do
                    if not rtIsMoving then break end
                    local char = player.Character local root = char and char:FindFirstChild("HumanoidRootPart")
                    if not root then rtIsMoving = false break end
                    
                    if i == 1 and not rtCurrentTween then root.CFrame = CFrame.new(targetPos + Vector3.new(0, 3, 0))
                    else
                        rtCurrentTween = TS:Create(root, TweenInfo.new(duration, Enum.EasingStyle.Linear), {CFrame = CFrame.new(targetPos + Vector3.new(0, 3, 0))})
                        rtCurrentTween:Play() rtCurrentTween.Completed:Wait()
                    end
                end
                if not rtIsLooping then rtIsMoving = false break end
            end
            showNotice("Маршрут завершен.")
        end)
    end, Color3.fromRGB(40, 160, 60), 15)
end

-- ================== 7. НАСТРОЙКИ И КОНФИГИ ==================
do
    CreateLabel(vSetts, "Полезные Утилиты (Оптимизация)")
    CreateToggleWithBind(vSetts, "🛡️ Anti-AFK (Анти-кик)", "Setts_AntiAFK", false, function(state)
        -- Обрабатывается в global loop
    end)
    
    player.Idled:Connect(function()
        if mySetts.toggles["🛡️ Anti-AFK (Анти-кик)"] then
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end
    end)

    local fpsBoosterConn
    CreateToggleWithBind(vSetts, "🥔 FPS Booster (Potato Mode)", "Setts_FPS", false, function(state)
        if state then
            local function applyPotato(v)
                pcall(function()
                    if v:IsA("BasePart") then
                        v.Material = Enum.Material.SmoothPlastic
                        v.Reflectance = 0
                    elseif v:IsA("Decal") or v:IsA("Texture") then
                        v.Transparency = 1
                    elseif v:IsA("ParticleEmitter") or v:IsA("Trail") then
                        v.Enabled = false
                    end
                end)
            end
            
            for _, v in ipairs(workspace:GetDescendants()) do
                applyPotato(v)
            end
            Lighting.GlobalShadows = false
            
            -- Шпион за новыми объектами (когда загружается новая карта)
            fpsBoosterConn = workspace.DescendantAdded:Connect(applyPotato)
            showNotice("FPS Booster включен (работает на новые карты)!")
        else
            if fpsBoosterConn then fpsBoosterConn:Disconnect() fpsBoosterConn = nil end
            showNotice("FPS Booster отключен (новые карты будут с текстурами).")
        end
    end)
    
    CreateActionRow(vSetts, "FOV (по умолч. 70)", "Установить FOV", function(val)
        local fov = tonumber(val)
        if fov then
            customFov = fov
            isFovEnabled = true
            showNotice("FOV установлен на: " .. fov)
        end
    end, "Сброс", function()
        isFovEnabled = false
        workspace.CurrentCamera.FieldOfView = 70
        showNotice("FOV сброшен до 70")
    end)

    CreateLabel(vSetts, "Внешние Скрипты (Запуск)")
    CreateButton(vSetts, "🎯 KutomAimbot (Не для MM2)", function()
        pcall(function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/Egorzq/KutomHub/refs/heads/main/KutomAimbot"))()
            showNotice("KutomAimbot запущен!")
        end)
    end, Color3.fromRGB(150, 50, 200))

    CreateLabel(vSetts, "Конфиг (Файлы)")
    CreateActionRow(vSetts, "Имя конфига...", "💾 Сохранить", function(val)
        if val ~= "" and writefile then
            pcall(function() writefile(val..".json", Http:JSONEncode({customAnims=customAnims, customPacks=customPacks, binds=binds, mySetts=mySetts, mm2Tracks=mm2Tracks, allTeleports=allTeleports})) end) showNotice("Конфиг "..val.." сохранен!")
        end
    end, "📂 Загрузить", function(val)
        if val ~= "" and readfile and isfile and isfile(val..".json") then
            pcall(function()
                local data = Http:JSONDecode(readfile(val..".json"))
                if data then 
                    customAnims = data.customAnims or {} customPacks = data.customPacks or {} binds = data.binds or {} mm2Tracks = data.mm2Tracks or mm2Tracks allTeleports = data.allTeleports or {}
                    if data.mySetts then for k, v in pairs(data.mySetts) do mySetts[k] = v end end
                    showNotice("Конфиг загружен! Перезайдите.") 
                end
            end)
        else showNotice("⚠️ Конфиг не найден!") end
    end)

    CreateLabel(vSetts, "Управление Сервером")
    CreateButton(vSetts, "🔄 Перезайти на сервер (Rejoin)", function()
        showNotice("Переподключение...")
        if #game:GetService("Players"):GetPlayers() <= 1 then
            player:Kick("\nRejoining...") task.wait(0.3) TPS:Teleport(game.PlaceId, player)
        else
            TPS:TeleportToPlaceInstance(game.PlaceId, game.JobId, player)
        end
    end, Color3.fromRGB(180, 100, 40))

    CreateButton(vSetts, "🌐 Сменить сервер (Server Hop)", function()
        showNotice("Поиск нового сервера...")
        task.spawn(function()
            local servers = {}
            local req = game:HttpGet("https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=Desc&limit=100&excludeFullGames=true")
            if req then
                local body = Http:JSONDecode(req)
                if body and body.data then
                    for i, v in next, body.data do
                        if type(v) == "table" and tonumber(v.playing) and tonumber(v.maxPlayers) and v.playing < v.maxPlayers and v.id ~= game.JobId then
                            table.insert(servers, 1, v.id)
                        end
                    end
                end
            end
            if #servers > 0 then TPS:TeleportToPlaceInstance(game.PlaceId, servers[math.random(1, #servers)], player)
            else showNotice("Не удалось найти другой сервер!") end
        end)
    end, Color3.fromRGB(150, 50, 200))

    CreateLabel(vSetts, "Визуал (Окружение)")
    
    local origAmbient = Lighting.Ambient
    local origOutdoorAmbient = Lighting.OutdoorAmbient
    local origBrightness = Lighting.Brightness
    local origClockTime = Lighting.ClockTime
    local origFogEnd = Lighting.FogEnd
    local origGlobalShadows = Lighting.GlobalShadows

    CreateToggleWithBind(vSetts, "☁️ Приглушенный свет (Dim Light)", "Setts_DimLight", false, function(state)
        if state then
            Lighting.Ambient = Color3.fromRGB(60, 60, 60)
            Lighting.OutdoorAmbient = Color3.fromRGB(50, 50, 50)
            Lighting.Brightness = 0.2
        else
            Lighting.Ambient = origAmbient
            Lighting.OutdoorAmbient = origOutdoorAmbient
            Lighting.Brightness = origBrightness
        end
    end)

    local playerLightConn
    CreateToggleWithBind(vSetts, "🔦 Свет только от игрока (Хоррор)", "Setts_PlayerLight", false, function(state)
        if state then
            Lighting.Ambient = Color3.fromRGB(0, 0, 0)
            Lighting.OutdoorAmbient = Color3.fromRGB(0, 0, 0)
            Lighting.Brightness = 0
            Lighting.ClockTime = 0
            Lighting.FogEnd = 200 
            Lighting.GlobalShadows = true

            local function addLightToPlayer(char)
                if not char then return end
                local hrp = char:WaitForChild("HumanoidRootPart", 3)
                if hrp and not hrp:FindFirstChild("KutomPersonalLight") then
                    local light = Instance.new("PointLight")
                    light.Name = "KutomPersonalLight"
                    light.Color = Color3.fromRGB(240, 240, 255) 
                    light.Range = 45 
                    light.Brightness = 2.5
                    light.Shadows = true 
                    light.Parent = hrp
                end
            end

            addLightToPlayer(player.Character)

            if playerLightConn then playerLightConn:Disconnect() end
            playerLightConn = player.CharacterAdded:Connect(function(char)
                addLightToPlayer(char)
            end)
        else
            Lighting.Ambient = origAmbient
            Lighting.OutdoorAmbient = origOutdoorAmbient
            Lighting.Brightness = origBrightness
            Lighting.ClockTime = origClockTime
            Lighting.FogEnd = origFogEnd
            Lighting.GlobalShadows = origGlobalShadows

            if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                local l = player.Character.HumanoidRootPart:FindFirstChild("KutomPersonalLight")
                if l then l:Destroy() end
            end
            
            if playerLightConn then playerLightConn:Disconnect() playerLightConn = nil end
        end
    end)

    CreateToggleWithBind(vSetts, "☀️ Fullbright (Яркий свет)", "Setts_Fullbright", false, function(state)
        if state then
            Lighting.Ambient = Color3.fromRGB(255, 255, 255) Lighting.OutdoorAmbient = Color3.fromRGB(255, 255, 255)
            Lighting.Brightness = 2 Lighting.ClockTime = 14 Lighting.FogEnd = 100000 Lighting.GlobalShadows = false
        else
            Lighting.Ambient = origAmbient Lighting.OutdoorAmbient = origOutdoorAmbient
            Lighting.Brightness = origBrightness Lighting.ClockTime = origClockTime Lighting.FogEnd = origFogEnd Lighting.GlobalShadows = origGlobalShadows
        end
    end)

    CreateToggleWithBind(vSetts, "🌌 Ночное небо (Night Sky)", "Setts_NightSky", false, function(state)
        if state then
            Lighting.ClockTime = 0
            if not Lighting:FindFirstChild("KutomNightSky") then
                local sky = Instance.new("Sky")
                sky.Name = "KutomNightSky"
                sky.SkyboxBk = "rbxassetid://1045964490"
                sky.SkyboxDn = "rbxassetid://1045964368"
                sky.SkyboxFt = "rbxassetid://1045964655"
                sky.SkyboxLf = "rbxassetid://1045964655"
                sky.SkyboxRt = "rbxassetid://1045964655"
                sky.SkyboxUp = "rbxassetid://1045962969"
                sky.StarCount = 3000
                sky.Parent = Lighting
            end
        else
            Lighting.ClockTime = 14
            if Lighting:FindFirstChild("KutomNightSky") then
                Lighting.KutomNightSky:Destroy()
            end
        end
    end)

    CreateToggleWithBind(vSetts, "✨ Красивые шейдеры (RTX)", "Setts_Shaders", false, function(state)
        if state then
            if not Lighting:FindFirstChild("KutomShadersBloom") then
                local bloom = Instance.new("BloomEffect", Lighting) bloom.Name = "KutomShadersBloom" bloom.Intensity = 0.1 bloom.Size = 24 bloom.Threshold = 2
                local cc = Instance.new("ColorCorrectionEffect", Lighting) cc.Name = "KutomShadersCC" cc.Brightness = 0.05 cc.Contrast = 0.1 cc.Saturation = 0.1 cc.TintColor = Color3.fromRGB(255, 224, 219)
                local sun = Instance.new("SunRaysEffect", Lighting) sun.Name = "KutomShadersSun" sun.Intensity = 0.05 sun.Spread = 0.8
                local blur = Instance.new("BlurEffect", Lighting) blur.Name = "KutomShadersBlur" blur.Size = 2
            end
        else
            if Lighting:FindFirstChild("KutomShadersBloom") then Lighting.KutomShadersBloom:Destroy() end
            if Lighting:FindFirstChild("KutomShadersCC") then Lighting.KutomShadersCC:Destroy() end
            if Lighting:FindFirstChild("KutomShadersSun") then Lighting.KutomShadersSun:Destroy() end
            if Lighting:FindFirstChild("KutomShadersBlur") then Lighting.KutomShadersBlur:Destroy() end
        end
    end)

    CreateToggleWithBind(vSetts, "✅ Радужный логотип", "Setts_Rainbow", mySetts.rainbow, function(state)
        mySetts.rainbow = state
        if not mySetts.rainbow then logoLabel.TextColor3 = Color3.fromRGB(220, 100, 60) end saveData()
    end)

    make("TextButton", {Size = UDim2.new(0, 90, 0, 25), BackgroundTransparency = 1, TextColor3 = Color3.fromRGB(65, 65, 80), Text = "By Kutom", Font = Enum.Font.GothamBold, TextSize = 11, TextXAlignment = Enum.TextXAlignment.Left}, vSetts)
end

-- ГЛОБАЛЬНАЯ Радуга
RS.Heartbeat:Connect(function()
    if mySetts.rainbow and logoLabel then logoLabel.TextColor3 = Color3.fromHSV(tick() % 5 / 5, 0.8, 1) end
end)

switchTab("Emotes")
mainFrame.Visible = true
